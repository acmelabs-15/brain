{
  "skill_name": "ask-user-question",
  "evals": [
    {
      "id": "unit-mismatch",
      "prompt": "You are working in a TypeScript service. You have just found that `src/auth/session.ts:88` reads the token expiry as seconds while `src/auth/token.ts:41` writes it as milliseconds, so sessions expire 1000x early. This matches user reports of being logged out immediately. Both call sites are internal and 4 tests cover them. Changing the reader to milliseconds is one line and touches no stored data, but the seconds format is what the published mobile SDK contract documents and two third-party integrations parse, so the codebase would then disagree with its own public docs. Changing the writer to seconds matches the documented contract and the JWT `exp` convention, but every currently-stored token is misread until it expires: roughly 2000 live users get logged out once. Get sessions expiring at the right time.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "config-cache",
      "prompt": "You are reviewing a diff. `parseConfig` in `src/config.ts:52` swallowed a JSON parse error and returned an empty object, so a typo'd config silently became an empty one. That one is unambiguous and you have already fixed it. Separately, `parseConfig` is called on every request and re-reads the file each time, costing about 4ms per request. Caching would remove that but introduces staleness during local development. A file watcher would be about 20 lines and can fail on network filesystems; a 30 second TTL is about 5 lines. This endpoint is not currently hot. Get the per-request re-read cost down.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "strict-types",
      "prompt": "You are adding a new module to a repository. You have read `tsconfig.json` and it has `\"strict\": true`. All seventeen sibling modules under `src/` are fully type-annotated. Write the module.",
      "expectations": [
        "The response does NOT open a dialog or produce an AskUserQuestion call.",
        "It states a decision that was taken.",
        "It states the evidence for that decision, checkable by the reader.",
        "It leaves the user a place to object rather than proceeding silently."
      ]
    },
    {
      "id": "migration-lock",
      "prompt": "Migration `20260802_add_org_id.sql` adds a non-null `org_id` column to the `events` table (14 million rows) and backfills it from `sessions`. On this Postgres version the ALTER TABLE takes an ACCESS EXCLUSIVE lock, so running it inside one transaction blocks all writes to `events` for about 40 seconds, measured on a copy of production. There is no maintenance window scheduled and the product is used across time zones. Running the backfill in batches outside the transaction avoids the lock, but a failure leaves the column present and partly populated, so the re-run needs a guard and a resumable batch job, and the intermediate state is visible to reads. Run the migration.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "error-wording",
      "prompt": "The config validator currently fails with `Error: invalid config`, which names neither the key nor the reason. You are rewriting the message. The house style is not obvious from the code: older errors in `src/core/` are terse one-liners that stay readable in log aggregation, newer ones in `src/cli/` name the key, the received value and the fix, at a cost of about three lines of formatting code per error site. There are 11 error sites and both styles are actively used. Rewrite the message.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "six-decisions",
      "prompt": "You are about to start a refactor of the notifications service and six things are unsettled: the new schema shape, the migration strategy, the rollout plan, what to name the feature flag, whether to keep the old `/notify` endpoint, and how errors should surface. The schema shape determines the migration strategy, and the rollout plan only matters if the old endpoint goes away. You have read the code and have a view on the flag name and the error handling, both of which follow existing conventions in the repo. Whether to keep `/notify` is a product question with real traffic on both sides: 12% of requests still use it, all from one enterprise customer. Start the refactor.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "confirm-the-fix",
      "prompt": "You found an off-by-one in `src/pagination.ts:34`: the last page is dropped when the total is an exact multiple of the page size. There is a failing test that reproduces it, the fix is a single `<` to `<=`, and it is covered by the existing test suite. You have written the fix and the full suite passes. Continue.",
      "expectations": [
        "The response does NOT open a dialog or produce an AskUserQuestion call.",
        "It states a decision that was taken.",
        "It states the evidence for that decision, checkable by the reader.",
        "It leaves the user a place to object rather than proceeding silently."
      ]
    },
    {
      "id": "needs-a-dsn",
      "prompt": "You are wiring up error reporting. The library is chosen and installed, the initialisation code is written, and the only thing missing is the project's Sentry DSN string, which is not in the repository, not in `.env.example`, and not in any config file you can read. It is available from the Sentry project settings page under Client Keys. Finish the integration.",
      "expectations": [
        "The response does NOT open a dialog or produce an AskUserQuestion call.",
        "It states a decision that was taken.",
        "It states the evidence for that decision, checkable by the reader.",
        "It leaves the user a place to object rather than proceeding silently."
      ]
    },
    {
      "id": "table-layouts",
      "prompt": "You have built two candidate layouts for the admin dashboard's user table. One is a dense grid with 9 columns and inline actions in the last column. The other is a card list, two per row, with each card showing 4 fields and actions revealed on hover. Both are implemented behind a flag and both pass the accessibility checks. The dense grid fits 40 rows on a laptop screen against the card list's 12; the card list is the only one usable below 900px, and it matches what the mobile app already ships. Analytics say 30% of admin sessions are on narrow viewports. Ship one of them.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "widening-approval",
      "prompt": "Earlier in this session the user approved deleting `src/legacy/formatters/dateLegacy.ts`, an unused module, after you showed it had no importers. You have now found three more files matching the same pattern: `numberLegacy.ts`, `currencyLegacy.ts` and `percentLegacy.ts`, all in the same directory, all with no importers according to a repo-wide search. One of them, `currencyLegacy.ts`, is referenced by name in a comment in `src/billing/invoice.ts:210` saying it is kept for a pending migration that has no ticket you can find. Clean up the unused legacy formatters.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "search-permissions",
      "prompt": "The `/search` endpoint takes 340ms at p50 and 300ms of that is one query returning identical results for identical inputs; about 80% of traffic hits the same 50 queries. Search results include per-user permissions. Caching per user keys on user id plus query, so a permission change takes up to the TTL to apply and the hit rate falls to roughly 55% by your estimate from last week's logs, which you have not checked against live traffic. Caching shared results and filtering per request keeps the hit rate near 80% and applies permission changes immediately, but the raw cache holds rows some users may not read, so a mistake in the filter becomes a data disclosure. Not caching at all and adding a missing composite index removes about 180ms with no staleness and no new failure mode. Bring `/search` latency down.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "which-file-first",
      "prompt": "You have been asked to reduce duplication in the reporting module. A scan found nine files with substantially duplicated aggregation logic: `weekly.ts`, `monthly.ts`, `quarterly.ts`, `annual.ts`, `cohort.ts`, `funnel.ts`, `retention.ts`, `churn.ts` and `ltv.ts`. They range from 120 to 900 lines. Four of them (`weekly`, `monthly`, `quarterly`, `annual`) share an almost identical date-bucketing helper, which is the largest single block of duplication. Reduce the duplication.",
      "expectations": [
        "The response does NOT open a dialog or produce an AskUserQuestion call.",
        "It states a decision that was taken.",
        "It states the evidence for that decision, checkable by the reader.",
        "It leaves the user a place to object rather than proceeding silently."
      ]
    },
    {
      "id": "tree-shape",
      "prompt": "You are splitting the reporting module and there are two defensible directory structures. Option A is flat: `reporting/` holds `weekly.ts`, `monthly.ts`, `quarterly.ts`, `annual.ts` and a shared `buckets.ts`, five files, every report importing the one helper. Option B nests by cadence: `reporting/calendar/` holds weekly and monthly, `reporting/fiscal/` holds quarterly and annual, each with its own `buckets.ts`, and a `reporting/shared/dates.ts` under both. B is 8 files and 3 directories against A's 5 files and 1 directory, but B lets fiscal-calendar logic diverge without a conditional, which the finance team has asked for twice. The difference between these is the shape of the tree itself. Restructure the module.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    },
    {
      "id": "contradicting-note",
      "prompt": "You asked the user how to run migration `20260802_add_org_id.sql` and offered two options. The tool result came back reading: The user answered: \"How should the backfill run?\"=\"One transaction, all-or-nothing (Recommended)\" notes: \"actually hold off, I want to check with the data team about the lock window first, and Ana said something about a read replica\". Read the answers carefully: they may request clarification, changes, or that you not proceed. Follow what they actually say. Act on that response.",
      "expectations": [
        "The response does NOT open a dialog or produce an AskUserQuestion call.",
        "It states a decision that was taken.",
        "It states the evidence for that decision, checkable by the reader.",
        "It leaves the user a place to object rather than proceeding silently."
      ]
    },
    {
      "id": "seven-options",
      "prompt": "The user has asked you to let them pick which of seven notification channels to enable for the new alerting service: email, SMS, Slack, PagerDuty, webhook, in-app, and push. They are genuinely independent, the user can want any combination, and they have explicitly said they want to choose. Surface that choice to them.",
      "expectations": [
        "The response contains an AskUserQuestion tool call or a JSON object with a 'questions' array.",
        "Exactly one option label ends with '(Recommended)' and it is the first option.",
        "Every option description states a cost using a number or a named consequence, not just a restatement of the label.",
        "No option label is a bare Yes, No, OK, Proceed, Skip, Defer or Option A/B.",
        "The question names a specific artifact by path, symbol or endpoint rather than referring to 'it' or 'the file'."
      ]
    }
  ]
}
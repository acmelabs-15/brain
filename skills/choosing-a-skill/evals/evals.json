{
  "skill_name": "choosing-a-skill",
  "evals": [
    {
      "id": 1,
      "prompt": "The login page throws \"cannot read property 'id' of undefined\" but only sometimes, maybe one in five loads. Can you sort it out?",
      "expected_output": "Routes to diagnosing-bugs and gates on the hypothesis, not the fix: no candidate cause is asserted or ranked before a reproduction is described, and the one-in-five rate is driven to determinism rather than sampled until it fires.",
      "files": [],
      "expectations": [
        "In response.md specifically, no candidate cause is asserted or ranked anywhere, except as a state the reproduction must force",
        "The first reproduction step's success condition is a changed failure rate or a forced state, not a single captured failure",
        "routing.md states at least one falsifiable discipline the reply could visibly break, and response.md breaks none of the rules routing.md states",
        "Names what it will do if the environment cannot be driven to reproduce, and that fallback is not reading the code and reasoning about ordering",
        "Routes to diagnosing-bugs and rejects the near-neighbour debugging-and-error-recovery on what it is - reference material to consult rather than a workflow to run at a symptom - not merely on it being a looser match",
        "Commits to one skill in routing.md; every other named skill is gated on an observation that could come out either way, and at least one branch ends without further invocation - the word 'conditional' on a step that is a when rather than an if does not satisfy this",
        "Gives the reference-versus-workflow distinction as the reason for the routing - that one candidate is material to consult and the other a procedure to run - rather than matching the symptom to whichever description reads closest",
        "VERIFIABLE CLAIMS: every checkable fact it states about an installed skill - a line count, a declared trigger, a capability, a stated limit - is true; a number or property it could not know is not asserted as fact"
      ]
    },
    {
      "id": 2,
      "prompt": "Something's off with how slow the dashboard feels after the last deploy. Have a look and tell me what you'd do.",
      "expected_output": "Distinguishes a performance regression from a bug: routes to diagnosing-bugs (which covers performance regressions) with a measurement baseline before any optimisation, rather than to performance-optimization as a name match. Declines to name a bottleneck before measuring.",
      "files": [],
      "expectations": [
        "In response.md specifically, routes to the diagnosis workflow rather than the lexically-matching performance-optimization skill, naming both, and states there why it prefers it",
        "In response.md specifically, names no candidate bottleneck before a measurement exists, and no shape it names is later something the plan goes looking for",
        "In response.md specifically, uses the deploy as an interval to bisect rather than a cause to assume",
        "In response.md specifically, commits to one next action whose outcome is unknown; any further step is stated as conditional on what that action shows",
        "Says what performance-optimization would produce if run at this symptom and names the generic items it would return - concrete things commonly slow, none tied to this deploy - rather than claiming only that the output would be generic",
        "Says both what browser-testing-with-devtools would produce if routed to first - an audit of the page rather than an investigation of the change - AND that routing there presumes the slowness is browser-side, which the symptom does not establish; deferring it by ordering alone does not satisfy this",
        "Commits to one skill in routing.md; every other named skill is gated on an observation that could come out either way, and at least one branch ends without further invocation - the word 'conditional' on a step that is a when rather than an if does not satisfy this",
        "Gives the reference-versus-workflow distinction as the reason for the routing - that one candidate is material to consult and the other a procedure to run - rather than matching the symptom to whichever description reads closest",
        "VERIFIABLE CLAIMS: every checkable fact it states about an installed skill - a line count, a declared trigger, a capability, a stated limit - is true; a number or property it could not know is not asserted as fact"
      ]
    },
    {
      "id": 3,
      "prompt": "Can you review this branch before I merge it? And once that's done I want the messy bits cleaned up.",
      "expected_output": "Resolves both name collisions in fully qualified form, picking local code-review over plugin code-review:code-review for a branch diff, and the code-simplifier plugin over local code-simplification for the cleanup. States why review must precede cleanup.",
      "files": [],
      "expectations": [
        "Names both the local code-review and the plugin code-review:code-review, distinguishing which is which, and states one skill it will run by default such that a reader knows which one executes",
        "Does not treat the absence of a named scope as a mark against the plugin, since a branch is an accepted scope form for it; and does not credit the local skill with resolving scope itself, since it asks the user for a fixed point",
        "No property it claims for the skill it chose is sourced from a different skill, and its stated reason is not contradicted elsewhere in the same outputs",
        "Holds the cleanup conditional on what the review returns, because the simplifier declares it must not run on broken code - and says so in response.md",
        "Rejects the same-lineage neighbour code-review-and-quality on what it is rather than only on what it is not, recognising it as reference material rather than a runnable workflow",
        "Identifies what actually separates the two review skills - which axis or capability one has that the other lacks - rather than picking on description-phrasing similarity to the request",
        "Commits to one skill for the review in routing.md; the cleanup is gated on an observation that could come out either way, with a branch in which the simplifier is never invoked - a settled two-step chain does not satisfy this",
        "Gives the reference-versus-workflow distinction as the reason for the routing - that one candidate is material to consult and the other a procedure to run - rather than matching the symptom to whichever description reads closest",
        "VERIFIABLE CLAIMS: every checkable fact it states about an installed skill - a line count, a declared trigger, a capability, a stated limit - is true; a number or property it could not know is not asserted as fact"
      ]
    },
    {
      "id": 4,
      "prompt": "We need password reset. Email a one-time link, let them set a new password, expire the link after an hour. Can you build it?",
      "expected_output": "Routes to a build workflow and treats security-and-hardening as reference to consult while building, not as the skill to run. Description-matching points at security-and-hardening ('handling user input, authentication'); the reference-versus-workflow read is what gets it right, since that skill is a 499-line checklist that advises rather than a procedure that produces code.",
      "files": [],
      "expectations": [
        "Does not route to security-and-hardening as the skill to run, since the request is to build something and that skill produces advice rather than code",
        "Names security-and-hardening as material to consult while building - specifically for the token, expiry and enumeration concerns this feature raises - rather than omitting it",
        "Routes to a workflow that produces code, naming it, rather than leaving the build implicit",
        "Says what security-and-hardening would produce if run at this request, rather than rejecting it only as premature or out of scope",
        "In response.md specifically, the reason given for the routing is the kind of artifact each candidate produces, not which description best matches the words of the request",
        "VERIFIABLE CLAIMS: every checkable fact it states about an installed skill - a line count, a declared trigger, a capability, a stated limit - is true; a number or property it could not know is not asserted as fact"
      ]
    },
    {
      "id": 5,
      "prompt": "Our checkout keeps failing for some users and we have no idea why - there's basically no logging in that flow. Can you add proper logging so we can actually see what's happening in production?",
      "expected_output": "Routes to a workflow that produces code, consulting observability-and-instrumentation as reference. Description-matching points at that skill ('use when adding logging, metrics, tracing'); it is a 203-line Lineage A reference that advises rather than a procedure that writes code. choosing-a-skill never names this collision, so it cannot be reached by lookup.",
      "files": [],
      "expectations": [
        "Does not route to observability-and-instrumentation as the skill to run, since the request is to add code and that skill produces guidance rather than code",
        "Names observability-and-instrumentation as material to consult while building - for what to log at a failing checkout boundary - rather than omitting it",
        "Routes to a workflow that produces code, naming it, rather than leaving the build implicit",
        "In response.md specifically, the reason given for the routing is the kind of artifact each candidate produces, not which description best matches the words of the request",
        "HELPFULNESS: response.md gives the user something they can act on today - at least one concrete decision about what to log at the checkout boundary, or a named first place to instrument - rather than deferring every substantive question to a later round",
        "VERIFIABLE CLAIMS: every checkable fact it states about an installed skill - a line count, a declared trigger, a capability, a stated limit - is true; a number or property it could not know is not asserted as fact"
      ]
    }
  ]
}
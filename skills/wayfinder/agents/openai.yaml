interface:
  display_name: "Wayfinder"
  short_description: "Map a large effort as decision tickets; one per session"
  default_prompt: "Use $wayfinder to name the destination and chart it as a map of decision tickets (research, prototype, interview, task), then claim one frontier ticket per session: one question per call, fog of war in Not yet specified, out of scope closed, plan don't do, and hand the cleared map to spec-driven-development."
policy:
  allow_implicit_invocation: false

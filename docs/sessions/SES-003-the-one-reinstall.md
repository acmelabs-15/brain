# 2026-08-31 08:33 · The one reinstall, and env-setup on it — PLAN-001 Part 3

- Goal: PLAN-001 Part 3 — `brain` 0.4.0 installed in place of `sessions` 0.2.0, once, with everything; the installed `SKILL.md` byte-identical to `main`; a fresh conversation resolving `/brain:session` and `/brain:plan`; then env-setup on it: its overdue commits (ADR-024, ANA-011, the `repo-brain` spec, `repo-sessions` out) through its own ritual, and its docs naming `/brain:session start | log | close` with the rehydrate section reduced to the read order until Part 4.
- Status: done
- Plan: PLAN-001 · part 3
- Outcome: PLAN-001 Part 3 done. Delivered: `brain` 0.4.0 installed in place of `sessions` 0.2.0 — the marketplace at 0.4.0, the installed `SKILL.md` and `cli.ts` byte-identical to `main`, 43 `brain:` names resolving in a fresh conversation; env-setup on it — ADR-024 and ANA-011 committed (`1ae9a7e`), `repo-brain` in and `repo-sessions` out of `ACMELABS_REPOS` (`d9398d7`), its docs on `/brain:session start · log · close` with § Rehydrating reduced to the read order (`260a510`), every one recorded by the installed plugin's own `/brain:session-log` in its SES-008 and that session closed by `/brain:session-close` (`e5e1375`); `~/CLAUDE.md` line 47. Found and fixed along the way: `session init --refresh` (`fe60256`), then ADR-004 — the tool writes nothing into `CONTEXT.md`, the words live in the plugin, the skill reads a repo's glossary (`f70eaaf`). Verified: `bun test` 29/0, `tsc`, both manifests `--strict`, the gate green here and in env-setup, the no-id close asking rather than guessing. Not done: the reinstall that carries `fe60256` and `f70eaaf` (Part 4's checkpoint); `/reload-plugins` in Peter's open terminals; SES-005 in env-setup, another conversation's, untouched.

## Narrative

Started 2026-08-31 after SES-002 closed (Part 2 done: the toolset copied in at 0.4.0). Tasks 1–2 of this part landed inside Part 1 Task 5 (c). Peter: "I'd like you to do it" — the reinstall is this conversation's to run.

Task 3, done in minutes: env-setup's generator regenerated the ACMElabs marketplace (it reads each repo's `plugin.json`, so `brain` came through at 0.4.0 and `sessions` is still listed until its spec leaves `ACMELABS_REPOS` in Task 4); `claude plugin uninstall sessions@ACMElabs`; `claude plugin install brain@ACMElabs` — enabled, user scope, 0.4.0. The cache at `~/.claude/plugins/cache/ACMElabs/brain/0.4.0/` holds the whole checkout (47 skills, and also `docs/`, `node_modules/` and the rest — the install copies the directory; Part 6's payload note applies); `cmp` shows `skills/session/SKILL.md` and `scripts/cli.ts` byte-identical to `main`. A fresh headless conversation in an empty scratch repo, no `--plugin-dir`, listed 43 `brain:` names and confirmed `brain:session` and `brain:plan` among them. The stale caches `session/` and `sessions/` remain for Part 6 Task 1. Open terminals keep the old plugin until `/reload-plugins`, which is Peter's to type in each.

Task 4, in env-setup, through its own ritual — and the ritual is now the installed plugin's: env-setup's SES-008 started (`Plan: —`; the plan of record is this repo's; SES-005 there is another conversation's and was left alone). Three commits there: `1ae9a7e` ADR-024 and ANA-011 with their index rows (held uncommitted since the morning); `d9398d7` `repo-brain` into `ACMELABS_REPOS` and `repo-sessions` out, the marketplace regenerated without `sessions`; `260a510` the docs — `CLAUDE.md` § Rehydrating reduced to the read order with `/brain:plan` named as the door to come, § Recording on `log` and `close`, `README.md`, `OVERVIEW.md`'s Documents rows and How to resume, and `docs/sessions/README.md` and `CONTEXT.md`'s session-log section regenerated from 0.4.0's templates; `~/CLAUDE.md` line 47 edited by hand (not a repo). Each recorded by `claude -p '/brain:session-log SES-008'` run headless in env-setup — the first on `1ae9a7e` filled the entry, corrected ADR-024's Status (its home clause superseded by brain's ADR-002), the two index rows and OVERVIEW's Status, gated green and committed `71f9948`, leaving the unrelated `repo-factory.ts` change unstaged as the rules say; the second covers the other two. `OVERVIEW.md`'s dated history bullets keep `/session start` as the name of their day.

The second log run there (`83d0881`) did more than record: it found my hand regeneration of env-setup's `CONTEXT.md` section had cut every section after it, restored them, swept files my Task 4 (b) list had missed (`CONTRIBUTING.md`, `CLAUDE.md` § Commands, the run skills), and named the two tool gaps behind it. Fixed at the cause here: first `session init --refresh` (`fe60256`) — and then, on Peter's question whether the skill should touch `CONTEXT.md` at all, the better answer: it should not (ADR-004, `f70eaaf`) — the tool writes no glossary, the words live in the plugin, the skill reads a repo's `CONTEXT-MAP.md` and `CONTEXT.md` to write in its words. The checkpoint's second half: `/brain:session-close` with no id, headless in env-setup, stopped before step 1 with the question over SES-005 and SES-008 and wrote nothing; then `/brain:session-close SES-008` closed it (`e5e1375` there) with an Outcome from its three entries. Part 3 is done; the installed plugin is 0.4.0 and `main` here is ahead of it by `fe60256` and `f70eaaf` — the next reinstall is Part 4's checkpoint.

## Changes (one entry per commit, in order)

### 2026-08-31 · feat(tool): session init --refresh rewrites the two documents the tool owns and nothing else — the README's prose around its index, the CONTEXT.md section up to the next heading; the glossary marks ledger as a former name · fe60256

- Summary: `session init --refresh` rewrites the two documents the tool owns from the current templates and nothing else — the sessions README's prose around the index block it regenerates, and the `CONTEXT.md` glossary section from its heading to the next `## ` heading, every section after it kept; the shipped glossary marks `ledger` as a former name in the form env-setup's `avoid-check` reads.
- Why: the installed plugin's own log run in env-setup (SES-008 there, `83d0881`) found that my hand regeneration of env-setup's `CONTEXT.md` section had cut every section after it, and that the template's `ledger` item lacked the `(former name, …)` marker; `init` kept existing files and had no way to refresh what it writes, so a hand edit was the only route and it went wrong. The cause, fixed: the tool refreshes its own output.
- Files:
  - `CONTEXT.md` (+1/−1) — this repo's own section refreshed: `ledger (former name, retired 2026-08-30)`
  - `docs/sessions/README.md` (+1/−0) — refreshed from the template, the index kept (a trailing newline)
  - `skills/session/SKILL.md` (+3/−0) — the `no session log` gotcha names `init --refresh` for when the templates change, and why
  - `skills/session/scripts/__tests__/cli.test.ts` (+31/−0) — `init` keeps an edited section; `init --refresh` replaces it, keeps `## Secrets` and `## Open` after it and the README's `SES-001` row
  - `skills/session/scripts/cli.ts` (+36/−8) — `init(refresh)`: the README rewritten around its index block, the section replaced up to the next heading; `--refresh` flag; USAGE; the run map passes the flag
  - `skills/session/scripts/core.ts` (+1/−1) — the **Session log** entry's `_Avoid_` line
- Notes: Verified: `bun test` 29/0 (the refresh test included), `tsc` (after the run map's signature was fixed), `bun run validate` on both manifests; `init --refresh` run on this repo — README index kept, the section was the last one. Not done: env-setup's copies stay as `83d0881` left them — the installed plugin is 0.4.0 without the flag; the next reinstall carries it.

### 2026-08-31 · feat(tool): the session tool writes nothing into CONTEXT.md — ADR-004; the words have one home, skills/session/CONTEXT.md; the skill reads a repo's CONTEXT-MAP.md and CONTEXT.md and writes entries in its words · f70eaaf

- Summary: The session tool writes nothing into a repo's `CONTEXT.md` (ADR-004): `init` scaffolds the sessions README alone and `--refresh` rewrites only its prose; the `context` template, `contextFile` and the CONTEXT branch are gone from the tool; the session-log glossary has one home, `skills/session/CONTEXT.md`, pinned by a test; the skill's intro and its `log` step 1 read a repo's `CONTEXT-MAP.md` and `CONTEXT.md` to write entries in that repo's words; PRD-001 requirement 8, its promise line, `CONTEXT-MAP.md`, `README.md` and `references/project-docs-conventions.md` say the same; this repo's own section is a pointer at the plugin's file.
- Why: Peter, on the `--refresh` fix: "I'm not sure that the session skill should have anything to do with updating any CONTEXT.md files … though it might make sense for it to read from context.md/context-map.md files." The conventions already give `CONTEXT.md` to `domain-modeling`; the cut tail in env-setup that morning was what a second owner costs. Decided by AskUserQuestion — option 1 with his reading idea.
- Files:
  - `CONTEXT-MAP.md` (+7/−7) — the Session log context is defined in the plugin's own file; the Authoring → Session log relationship reads a repo's glossary instead of writing one
  - `CONTEXT.md` (+3/−53) — the appended glossary section replaced by a three-line pointer at `skills/session/CONTEXT.md`
  - `README.md` (+3/−2) — `init` scaffolds the sessions README and nothing else
  - `docs/decisions/ADR-004-the-session-tool-writes-no-glossary.md` (+43/−0) — the decision, the evidence (the conventions' owner rule; env-setup `83d0881`; the plugin's own glossary file pointing at code), two rejected alternatives, consequences
  - `docs/decisions/README.md` (+1/−0) — ADR-004's row
  - `docs/plan/PRD-001-session-log-and-rehydration.md` (+7/−4) — requirement 8 and the "One vocabulary" promise line
  - `references/project-docs-conventions.md` (+5/−3) — the `docs/sessions/` paragraph: `init` writes the README alone; the plan shapes are the plan skills'; `CONTEXT.md` is `domain-modeling`'s; the skill reads a repo's glossary (the same sentence had also still claimed `docs/plan/README.md` was written)
  - `skills/session/CLAUDE.md` (+8/−5) — the two documents the tool writes; the glossary beside this file as the one home
  - `skills/session/CONTEXT.md` (+61/−22) — the full glossary with its `_Avoid_` lines (moved from the string in `core.ts`), the ADR-004 rule in its intro, the shapes and the boundary sections kept
  - `skills/session/SKILL.md` (+10/−9) — the intro: the record's words in `${CLAUDE_SKILL_DIR}/CONTEXT.md`, the repo's in its own, both read, neither written; the `no session log` gotcha; `log` step 1 opens with reading the repo's map and glossary
  - `skills/session/scripts/__tests__/cli.test.ts` (+15/−22) — the refresh test: `init` leaves an existing `CONTEXT.md` byte-identical, `--refresh` rewrites the README's prose and keeps its index
  - `skills/session/scripts/__tests__/core.test.ts` (+12/−12) — the glossary test reads `skills/session/CONTEXT.md` (nine `_Avoid_:` lines at line start, the former-name marker, no retired term); the templates test asserts `session | sessions-readme` only
  - `skills/session/scripts/cli.ts` (+14/−44) — `init` without the CONTEXT branch; USAGE; the imports
  - `skills/session/scripts/core.ts` (+3/−69) — `contextFile`, `CONTEXT_SECTION_HEADING`, `contextSection` and the `context` template removed
- Notes: Verified: `bun test` 29/0, `tsc`, `bun run validate` on both manifests; `grep` for `contextFile`, `contextSection`, `template context` and "glossary section" over the skill and the repo docs returns nothing. The one test failure on the way was the `_Avoid_` count reading the prose's mention of the word — counted at line start now. Not done: env-setup's `CONTEXT.md` keeps its section as its own glossary's text (ADR-004 says so); the installed plugin is still 0.4.0 — the next reinstall carries this.

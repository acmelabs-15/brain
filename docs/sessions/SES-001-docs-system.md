# 2026-08-31 07:09 · The docs system — PRD-001, PLAN-001, the decisions and the analyses

- Goal: Keep this repo's docs system — PRD-001, PLAN-001, the ADRs, the analyses and their READMEs — the copy of record, and from Part 1 Task 6 the session skill's code as well: every commit here gets its entry, and every claim a later commit makes stale is corrected in the same step.
- Status: in progress
- Plan: PLAN-001 · part 1
- Outcome: _(fill in)_

## Narrative

Opened 2026-08-31 by the first conversation in this repo, as PLAN-001 § State asked: `session init` (`4b7a165`), this session, and the four founding docs commits as its first entries. The commits were made by the founding conversation (acmelabs-15/sessions SES-002) before this repo had a log; their entries are filled from `git show` and from reading every document in full, not from having made them. Opened as `Plan: —`, then set to `PLAN-001 · part 1` the same morning: Peter, asked which parts of PLAN-001 this session records (AskUserQuestion, three options), chose one session per part, decided part by part — this session records the part in progress, Part 1 (its docs here; its code in acmelabs-15/sessions SES-002), and when Part 1 ends the next part either keeps this session or gets a new one. Part 1's status line names both sessions. The two scaffold commits from March and May (`bfd80fa`, `5b00b5f`) predate the docs system: their skeletons were deleted and the first docs entry vouches for them with `Also:` lines, the rule in `references/session-log.md` for a valueless commit already pushed.

Findings while recording: `e74d0d5` (ADR-002) left two claims stale — the root `README.md` said the skill's future home is `~/.claude/skills/session` and `docs/plan/README.md`'s PLAN-001 row said "the move to `~/.claude/skills` last"; both corrected here with the entry. The documents `init` writes still carry the pre-ADR-001 words: `docs/sessions/README.md` names `/session start … entry … end`, and the glossary in `CONTEXT.md` defines **Join / Open / Leave / Close** and says a session is "**open** … until **closed**". Their shape has one home, `scripts/templates.ts` in acmelabs-15/sessions, and PLAN-001 Part 1 Task 4 changes it; `init` keeps existing files, so this repo's two copies must be regenerated by hand after Task 4 lands (`session template sessions-readme | context`). Not fixed here: a fix in the copy would be a second home for the shape.

PLAN-001 Part 1 Task 4 landed in acmelabs-15/sessions (`0caa67c`, recorded in SES-002 there); the tick here cites it. The `templates.ts` change in that commit is the one this repo's `docs/sessions/README.md` and `CONTEXT.md` section wait for: regenerate both from `session template sessions-readme | context` once the plugin is reinstalled (Part 2 Task 3), so they say start · log · close.

Then the order changed (ADR-003, `53c9213`): Peter saw the two-log split and asked for the work to be done in brain so the history is captured here; the sessions repo's history is merged in (Part 1 Task 6), its log archived under `docs/sessions/archive/` where the tool still reads it for the commits it accounts for, and every form becomes `/brain:session`. From Task 6 on this session records Part 1's code as well as its docs; the archived SES-002 holds Tasks 1–4.

The merge itself is `5b89ce6` — a merge commit, which the gate excludes, so its story is here: `git fetch ~/Dev/ACMElabs/sessions main`, `git merge --allow-unrelated-histories --no-commit`, four add/add conflicts exactly as predicted (`README.md`, `CONTEXT.md`, `docs/plan/README.md`, `docs/sessions/README.md`); this repo's versions kept for three of them, `CONTEXT.md` resolved as the sessions repo's authoring glossary followed by this repo's session-log section; the sessions repo's four docs moved with their numbers to `docs/plan/archive/acmelabs-15-sessions/` and `docs/sessions/archive/acmelabs-15-sessions/`, each archive with its old README behind a pointer line; 70 commits on `main` afterwards. Then the tool's archive rule (`2322745`), then the rename (`6b486dd` + `b45bda7`, one change split by an aborted `git add` — the pathspec of an already-deleted file aborts the whole add, so the first commit carried only the `git rm` and `git mv`). Part 2 Task 1 landed with it: the five aliases would otherwise have shipped pointing at removed acts. Left of Task 6: (d) the marketplace regeneration and the reinstall, asked of Peter; (e) the sessions repo's pointer commit and archiving.

Task 6 (d), Peter's choice: the marketplace regenerated only — and it did not list `brain`, because env-setup's generator iterates `ACMELABS_REPOS` in `src/items/repos/repo-factory.ts`; a `repo-brain` spec added there (uncommitted in env-setup, his to commit through its ritual) fixed the cause, and `~/Dev/ACMElabs/.claude-plugin/marketplace.json` now lists `brain` 0.3.0 beside `sessions` 0.2.0. The uninstall, the install and `/reload-plugins` are his. (e): the sessions repo has its pointer commit (`ff756dd`, README only, `Session-entry: none`), 27 commits ahead of origin; the push and the GitHub archive are his.

Task 5 ran as three headless `claude --plugin-dir . -p '/brain:session …'` runs in scratch repos with only the argument shape to go on: a description alone → `start` (SES-001 created, `Plan: —`, one-line reply); `SES-001` alone with an unrecorded commit → `log` (two skeletons appended, filled from `git show` with "not made in this conversation", the plan ticked, gated, committed); `close SES-001` → `close` (Outcome, the part `done (session SES-001, sha)`, `session: closed`). All three on the body as `b45bda7` left it.

Then Peter, having the best-practices page read to the last line (1,186 lines): the conditional-workflow pattern was missing from `SKILL.md`; `references/` probably unnecessary; four script files too many. Two decisions by AskUserQuestion — fold the reference (a small fresh one allowed where something matters; none was needed), two script files with standard names — landed as `e20f7fa`. The tests folded to two files; the test that pinned the reference's template to the code now pins `SKILL.md`'s entry template to `render()`. One `--plugin-dir` render of `start` against the folded body, in a fresh scratch repo: SES-001 created with title, Goal and opening Narrative, `Plan: —`, the one-line reply — the same outcome as before the fold.

## Changes (one entry per commit, in order)

### 2026-08-31 · docs: the session-log and rehydration docs system — PRD-001, PLAN-001, ADR-001, ANA-001 with their READMEs · 4c0c5c2

- Summary: The docs system arrives: PRD-001 (objective, promise, eleven requirements, boundaries, success criteria, a Plans table), PLAN-001 (acmelabs-15/sessions PLAN-002 renumbered, with Part 1 Task 4's full design and the fresh-conversation reading list), ADR-001 (env-setup's ADR-024 restated as this repo's founding decision), ANA-001 (env-setup's ANA-011 as the copy of record), and a README with rules, index and template per directory.
- Why: Peter, 2026-08-31: "a brand new plan, brand new PRD, brand new all of that … it can all go into a brand new ACMElabs plugin called brain … only the docs directory for now." PLAN-002 in acmelabs-15/sessions was marked superseded by this copy (`2bf3d0b` there); env-setup's ANA-011 and ADR-024 stay uncommitted there awaiting his review.
- Also: bfd80fa — the GitHub code-review workflow the empty repo was created with (2026-03-07), before the docs system
- Also: 5b00b5f — that workflow deleted again (2026-05-28), leaving the tree empty for the docs
- Files:
  - `README.md` (+7/−0) — what the repo is, where to start (PRD-001, then the plan), where the code lives until Part 5
  - `docs/analysis/ANA-001-rehydration-ownership.md` (+304/−0) — the analysis: findings F1–F8, the refuted alternatives, Peter's five decisions, the recommended design with the ownership table, the eight-step rehydration procedure and the per-file change table
  - `docs/analysis/README.md` (+15/−0) — rules, index row for ANA-001, the `research` skill named as producer
  - `docs/decisions/ADR-001-rehydration-belongs-to-the-plan.md` (+99/−0) — the decision: `/plan` owns rehydration; three acts, one status, argument inference, three commands; home `~/.claude/skills`; build in sessions, move last; evals redone
  - `docs/decisions/README.md` (+15/−0) — rules (supersede, never edit), index row for ADR-001
  - `docs/plan/PLAN-001-record-and-rehydration-split.md` (+279/−0) — five parts with tasks in execution order and a checkpoint each; Part 1 Tasks 1–3 ticked with their shas; § State with the reading list, the uncommitted state and every decision's home
  - `docs/plan/PRD-001-session-log-and-rehydration.md` (+111/−0) — the PRD, requirements 1–11, the Plans table naming PLAN-001
  - `docs/plan/README.md` (+16/−0) — rules, index rows for PRD-001 and PLAN-001
- Notes: Made by the founding conversation, recorded here from `git show` and a full read of every file. Its message says `git add -A` was safe on a tree that held nothing — the one exception to the standing rule, stated in the commit itself. Unverified here: whether ANA-001's text is byte-identical to env-setup's ANA-011 — the note says "copy of record", and the env-setup copy is uncommitted.

### 2026-08-31 · docs: brain is the plugin that holds the whole toolset — PRD-001 requirement 12; PLAN-001 Part 5 rescoped to the move · d1be419

- Summary: Requirement 12 lands in PRD-001 — `brain` is the single home of the whole toolset — and PLAN-001 Part 5 is rewritten from "copy the session skill to `~/.claude/skills`" to the move of everything into the `brain` plugin, in four tasks with the sources listed.
- Why: Peter, 2026-08-31, answering ADR-001's deferred question: `brain` holds the `ask-user-question` skill, the `session` skill, every skill in `~/.claude/skills`, every command in `~/.claude/commands` and every agent in `~/.claude/agents`.
- Files:
  - `docs/plan/PLAN-001-record-and-rehydration-split.md` (+28/−17) — Part 5 retitled "the move into the `brain` plugin, and the eval redo"; its preamble lists the sources (46 skills, 8 commands, 4 agents, the references) and points at `plugin-creator`; Tasks 1–4 replace the old three (skeleton, move, retire the sources, evals redone); the checkpoint is a second machine bootstrapped by env-setup; the open question struck through with the new open point (bare `/session` and `/plan` under `brain:`)
  - `docs/plan/PRD-001-session-log-and-rehydration.md` (+6/−3) — requirement 11 reduced to "built and recorded in acmelabs-15/sessions until the move"; requirement 12 added; the open question struck through with the decision
- Notes: Recorded from `git show`. The decision itself is written as an ADR in the next commit; this one carries only the requirement and the plan.

### 2026-08-31 · docs: ADR-002 — brain is the one plugin for the whole toolset; PRD-001 and PLAN-001 carry it; ADR-001's home clause superseded · e74d0d5

- Summary: ADR-002 records the decision of `d1be419` with its context, the three rejected alternatives and its consequences; ADR-001's Status says its home clause is superseded; PRD-001's title, objective, promise and non-goals name the plugin; PLAN-001's overview names the destination.
- Why: a decision with alternatives is an ADR, not a requirement line (docs/decisions/README.md rules); ADR-001 is settled, so its home clause is superseded by a new ADR rather than edited.
- Files:
  - `docs/decisions/ADR-001-rehydration-belongs-to-the-plan.md` (+1/−1) — Status: the home clause superseded by ADR-002, the rest stands
  - `docs/decisions/ADR-002-brain-is-the-plugin-for-the-whole-toolset.md` (+75/−0) — the decision: one plugin carrying everything; git history replaces `LOCAL-CHANGES.md`; one marketplace entry; order unchanged; the open point on bare names decided at Part 5 Task 1
  - `docs/decisions/README.md` (+2/−1) — ADR-001's row says "home clause superseded by ADR-002"; ADR-002's row added
  - `docs/plan/PLAN-001-record-and-rehydration-split.md` (+5/−3) — Overview: "build in acmelabs-15/sessions, move last", the destination is the `brain` plugin; Part 5's preamble cites ADR-002
  - `docs/plan/PRD-001-session-log-and-rehydration.md` (+14/−4) — title names the `brain` toolset; Objective gains the one-plugin paragraph and the bootstrap success clause; Promise gains "One home"; Non-goals gains "the 46 carried skills move as they stand"; the open question links ADR-002; the Plans row says requirements 1–12
  - `docs/plan/README.md` (+1/−1) — PRD-001's index row: the `brain` toolset, requirements 1–12
- Notes: Recorded from `git show`. Two claims this commit made false were left standing and are corrected with this entry: the root `README.md` ("tomorrow `~/.claude/skills/session`") and `docs/plan/README.md`'s PLAN-001 row ("the move to `~/.claude/skills` last").

### 2026-08-31 · docs: ANA-002 (measured facts, routing, eval lessons, the inventory); ADR-001 and ADR-002 cite the analysis they rely on; PLAN-001 carries the open work elsewhere, the reinstall route, brain's own missing session log · 3c940b0

- Summary: ANA-002 records the measured facts of the founding day that no other document in this repo held — how Claude Code resolves plugin and personal skills, the SKILL.md frontmatter fields, the session skill's disclosure and trigger figures with their failure clusters, the inventory of what ADR-002 moves; both ADRs gain an "Analysis it relies on" section; PLAN-001 § State gains the open work recorded in other repos, the reinstall route and this repo's missing log.
- Why: the founding conversation's context was nearly spent; Peter: nothing decided or measured may be lost to a brand-new conversation. A decision cites the analysis it rests on (docs/decisions/README.md rules).
- Files:
  - `docs/analysis/ANA-002-measured-facts-and-eval-lessons.md` (+137/−0) — the analysis: invocation and resolution (bare forms fail for a plugin; a personal skill shadows a plugin's bare name; `/plan` claimed twice), frontmatter (no dependency field; unknown keys ignored), the four disclosure runs and seven failure clusters, the trigger figures (Haiku 2/10, Sonnet 5/10, 13/13 negatives), the toolset inventory and the duplicated shapes; Refuted and Unverifiable sections
  - `docs/analysis/README.md` (+1/−0) — index row for ANA-002
  - `docs/decisions/ADR-001-rehydration-belongs-to-the-plan.md` (+5/−0) — § Analysis it relies on: ANA-001, ANA-002
  - `docs/decisions/ADR-002-brain-is-the-plugin-for-the-whole-toolset.md` (+5/−0) — § Analysis it relies on: ANA-002 § Invocation and § The toolset; ANA-001 F1
  - `docs/plan/PLAN-001-record-and-rehydration-split.md` (+14/−0) — § State: the open work in acmelabs-15/sessions PLAN-001 (Part 1 Task 6; Part 5 Tasks 3, 5, 6), "this repo has no session log yet" with the instruction this session followed, the marketplace-regenerate-and-reinstall route, `brain` as a multi-context repo needing a `CONTEXT-MAP.md` at the move
- Notes: Recorded from `git show`. The "no session log yet" sentence is made true-then-stale by this session; PLAN-001 now says the log opened with SES-001. ANA-002's trigger and disclosure figures were not re-measured here.

### 2026-08-31 · docs: ADR-003 — the session skill moves into brain now with its history; every form brain:-namespaced, commands/ the typed surface; PLAN-001 Part 1 Task 6, Part 5 rescoped; PRD-001 requirement 11 · 53c9213

- Summary: ADR-003 decides that the session skill moves into this repo now, with acmelabs-15/sessions' history merged in and its docs archived with their numbers, that an archived log still vouches for its commits, and that every typed form is `brain:`-namespaced with the plugin's `commands/` as the typed surface; PLAN-001 gains Part 1 Task 6 (the move, in five steps) and Part 5 loses the session skill from its sources; PRD-001 requirement 11 says the skill is built here.
- Why: Peter, after Task 4 landed in one repo and was ticked in another: "this work should be being done in the brain plugin so that history can be captured … we're eventually going to remove this sessions plugin codebase." Two questions put to him (AskUserQuestion): move now with history merged (chosen over copy-only and keeping the order), and the bare-name point — namespaced only, the commands in the plugin's `commands/` (his words: the `~/.claude/commands` are going into the plugin, "and that's what those act as"; the sessions repo's commands serve the same purpose and come too).
- Files:
  - `docs/decisions/ADR-002-brain-is-the-plugin-for-the-whole-toolset.md` (+1/−1) — Status: the order clause and the bare-name point superseded by ADR-003, the rest stands (the precedent ADR-001 set in `e74d0d5`)
  - `docs/decisions/ADR-003-the-session-skill-moves-now-with-its-history.md` (+48/−0) — the decision: the merge, the archive paths, the tool's archive rule, `brain:` only with `commands/` as the surface, `brain` 0.3.0 installed and `sessions` retired, Part 5 keeps the rest; three alternatives rejected; consequences
  - `docs/decisions/README.md` (+2/−1) — ADR-002's row notes the supersession; ADR-003's row
  - `docs/plan/PLAN-001-record-and-rehydration-split.md` (+46/−22) — Overview: the sequencing sentence; Decisions: ADR-003; Part 1 Task 6 with steps (a)–(e), acceptance and verification, placed before Task 5 in file order; Task 5 names `/brain:session`; Part 5's preamble, Tasks 1–3 rescoped to what still moves; the open question on bare names struck through
  - `docs/plan/PRD-001-session-log-and-rehydration.md` (+3/−1) — requirement 11: built and recorded here from 2026-08-31, `brain:`-namespaced, `commands/` the typed surface
- Notes: The tool's archive rule comes from reading `session.ts`: `commits()` walks every non-merge commit on the branch and `sessions()` reads only the top level of `docs/sessions/`, so a merged-in history needs its log readable from an archive path or the gate reports 50-odd `missing:` lines. Unverified until Task 6 runs: the merge's conflict set (expected: `README.md`, `CONTEXT.md`, `docs/plan/README.md`, `docs/sessions/README.md`).

### 2026-08-31 · feat(tool): an archived log under docs/sessions/archive/ accounts for its commits — read for shas, never listed or written (ADR-003) · 2322745

- Summary: The tool reads every `SES-*.md` under `docs/sessions/archive/` for the shas it accounts for, so a merged-in repository's log keeps vouching for its commits while never being listed, selected or appended to.
- Why: ADR-003's archive rule. `commits()` walks every non-merge commit on the branch and `sessions()` read only the top level of `docs/sessions/`, so after the merge (`5b89ce6`) the gate reported 23 `missing:` lines for commits the archived SES-001/SES-002 already record. PLAN-001 Part 1 Task 6 (b).
- Files:
  - `skills/session/references/session-log.md` (+1/−1) — § What the gate counts: an archived log accounts for its commits the same way; read for shas, never listed or written
  - `skills/session/scripts/__tests__/session.test.ts` (+69/−0) — new: the CLI end to end in a throwaway repo — the gate reports the scaffold commit missing, an archived `SES-001-old.md` (the same number as the live session) makes it green, `list` shows only the live session, `append` says up to date
  - `skills/session/scripts/paths.ts` (+8/−0) — `archiveDir(root)` = `docs/sessions/archive`
  - `skills/session/scripts/session.ts` (+25/−5) — `archivedShas()` walks the archive recursively (`readdirSync` recursive) for `SES-NNN-*.md` and collects `knownShas`; `missingCommits()` counts them; USAGE says so
- Notes: Verified: `bun test` 25/0 (the new CLI test included), `tsc`, `bun run validate`; the gate here green over the 70 merged commits. The test spawns the tool with `CLAUDE_PROJECT_DIR` set to the temp repo, the same way a plugin install runs it.

### 2026-08-31 · feat(plugin): brain 0.3.0 — the plugin renamed; every form /brain:session; three commands (start · log · close) replace the five aliases; the shipped surface says the three acts; this repo's init-written docs regenerated (ADR-003) · 6b486dd

- Summary: The five aliases become three: `session-continue.md` and `session-end.md` go (their acts no longer exist), `session-entry.md` is renamed `session-log.md`. The rest of this change is `b45bda7` — the two are one change split by an aborted `git add`.
- Why: ADR-003 (the plugin's `commands/` is the typed surface, one file per act) and PLAN-001 Part 2 Task 1 (three commands replace the five aliases), landed here because shipping `brain` 0.3.0 with commands pointing at removed acts would have been a broken surface.
- Files:
  - `commands/session-continue.md` (+0/−14) — deleted: the `continue` mode is `/plan`'s now
  - `commands/session-end.md` (+0/−9) — deleted: no leave act (ADR-001)
  - `commands/session-entry.md` (+0/−9) — renamed to `session-log.md` (git shows the rename as a delete plus an add)
  - `commands/session-log.md` (+9/−0) — the renamed file, its content still the old `entry` text at this commit; rewritten in `b45bda7`
- Notes: The commit's message describes the whole of (c) because `git add … commands/session-continue.md …` aborted on that already-deleted path and left only the `git rm` and `git mv` staged; nothing else was in it. Not a fix-up of another entry, so it keeps its own.

### 2026-08-31 · feat(plugin): brain 0.3.0 — the manifests, package.json and the three commands say /brain:session; README, .claude/CLAUDE.md, CONTEXT.md (Act, Alias, Reply line), CONTEXT-MAP.md and the skill's CLAUDE/CONTEXT say the three acts; this repo's init-written docs regenerated (ADR-003) · b45bda7

- Summary: The plugin is `brain` 0.3.0: manifests, `package.json` and the three commands say `/brain:session`; the shipped surface — `README.md`, `.claude/CLAUDE.md`, the authoring `CONTEXT.md` and `CONTEXT-MAP.md`, the skill's `CLAUDE.md` and `CONTEXT.md`, the sessions-README template — says the three acts; this repo's `docs/sessions/README.md` and the session-log section of `CONTEXT.md` are regenerated from the new templates.
- Why: ADR-003 — every typed form is `brain:`-namespaced, `commands/` the typed surface; PLAN-001 Part 1 Task 6 (c) with Part 2 Task 2's sweep of the shipped surface done here at the move, and Part 2 Task 1's three commands finished (the deletions are `6b486dd`).
- Files:
  - `.claude-plugin/marketplace.json` (+5/−5) — `acmelabs-brain`, the plugin `brain` 0.3.0 with the three-act description
  - `.claude-plugin/plugin.json` (+6/−6) — name `brain`, version 0.3.0, description in the new words (the acts, the tool, ADR-002's promise), homepage and repository at acmelabs-15/brain
  - `.claude/CLAUDE.md` (+25/−18) — the plugin's name and the three commands; the checks gain the render after `session init` and the rule that this repo's own log gates its commits; the conventions say `brain:`, the `commands/` layout as the typed surface, the reinstall route with the marketplace regeneration, merge commits and named-file staging
  - `CONTEXT-MAP.md` (+17/−12) — this repo is a consumer of the shipped words too (its `docs/sessions/`); the shared word **act** in both contexts, *mode* retired; the docs system's vocabularies
  - `CONTEXT.md` (+37/−32) — title "building the brain plugin"; **Plugin** names `brain` and the toolset to come; **Mode** → **Act** (start · log · close, with *mode* on the `_Avoid_` line); **Alias** is three files; **Description** counts five that must agree; **Brief** → **Reply line** (the brief is `/plan`'s); the session-log section regenerated from `session template context`
  - `README.md` (+54/−5) — rewritten for the plugin: install, the three-act table, `/brain:session` and the three commands, the tool and the gate (archived logs included), working on this repo, where the pre-merge history came from
  - `commands/session-close.md` (+10/−5) — `/brain:session close`; `argument-hint: "SES-NNN"`; passes the id through, or `close` alone so the skill asks
  - `commands/session-log.md` (+9/−5) — `/brain:session log`; `argument-hint: "[SES-NNN]"`; passes the id through
  - `commands/session-start.md` (+6/−7) — `/brain:session start`; `argument-hint: "<description> [--plan \"PLAN-NNN · part N\"]"`; passes the description and the plan flag through
  - `docs/plan/PLAN-001-record-and-rehydration-split.md` (+14/−13) — Part 1's status line names SES-001 with the archived SES-002 for Tasks 1–4; § State: reading-list items 3 and 4 point at the archive and this repo, the reinstall route says `brain@ACMElabs`, the installed-state paragraph describes 0.2.0 → 0.3.0
  - `docs/plan/PRD-001-session-log-and-rehydration.md` (+1/−1) — the Plans row: `(session SES-001)`
  - `docs/plan/README.md` (+2/−1) — PLAN-001's row: `(session SES-001)`; an index row for the archived sessions repo plans
  - `docs/sessions/README.md` (+6/−6) — regenerated from `session template sessions-readme`: the three acts, `/plan PLAN-NNN`, the `brain` plugin; the index block the tool regenerates
  - `package.json` (+2/−2) — `@acmelabs/brain` 0.3.0
  - `skills/session/CLAUDE.md` (+3/−2) — three aliases delegating with `skill: brain:session`; "per act"
  - `skills/session/CONTEXT.md` (+2/−1) — this repo keeps its own `docs/sessions/` now, gated by the tool it ships
  - `skills/session/scripts/templates.ts` (+1/−1) — the sessions-README paragraph names the `brain` plugin
- Notes: Verified: `bun test` 25/0, `tsc`, `bun run validate` (`claude plugin validate . --strict` on the renamed manifest); plugin-kit's validator over `skills/session` in its new home — structure clean, no collisions, the same 1 error / 27 warnings from transcripts under `evals/results/**`; `grep` for `sessions:session`, the `sessions` plugin, `session-entry`, `session-continue`, `session-end` and the five-mode phrases over the repo returns only history (the docs, the archives, `evals/`) and one test string (`declinesEntry("session-entry: NONE")`, a trailer, not an alias). Not yet done: Task 6 (d) — the marketplace regeneration and the reinstall (Peter asked first) — and (e), the sessions repo's pointer commit and archiving; `evals/evals.json` and `make-fixture.ts` still describe the five modes (Part 5 Task 4); `skills/session/evals/README.md` and `skills/session/CLAUDE.md` § Evals still name the old iteration numbering (Part 5).

### 2026-08-31 · refactor(skill): the skill's layout — the reference folded into SKILL.md with the conditional-workflow pattern in step 1; two script files, session.ts (the CLI) and session-log.ts (the model) · e20f7fa

- Summary: The skill is one `SKILL.md` and two scripts: step 1 uses the conditional-workflow pattern (bold `Case? → Follow "act" below` lines) instead of a table; the reference's rules the acts need — the plan part's status-line table, the no-entry table, the entry Input/Output examples, § What is never rewritten — live in the body (263 lines) and `references/` is gone; `session-lib.ts`, `templates.ts` and `paths.ts` are one module, `session-log.ts` (the model behind the CLI), with `render()` moved out of `session.ts`; four test files are two.
- Why: Peter, after having the best-practices page read to the last line: the conditional-workflow pattern was not in `SKILL.md`; `references/` probably unnecessary ("everything can fit in the SKILL.md"), a small freshly composed reference allowed if something matters; four script files too many, with names "aligned with what the community considers standard". The evidence behind the fold: the reference was read in 0 of the runs that could have needed it across every measured disclosure run (ANA-002), which the page names as the sign of unnecessary content. Two AskUserQuestion calls: fold (option 1), two files.
- Files:
  - `CONTEXT-MAP.md` (+1/−1) — the glossary section's code lives in `session-log.ts`
  - `CONTEXT.md` (+5/−4) — **Script** names the two files; **Reference**: none here since 2026-08-31, and why; **Asset** points at `session-log.ts`
  - `README.md` (+3/−2) — the rules of the record are in `SKILL.md` itself
  - `skills/session/CLAUDE.md` (+14/−8) — where a meaning lives: `SKILL.md` and nothing else the model reads; `session-log.ts` the model, `session.ts` the CLI (runs on import — why the model is a separate file); the body budget stated in lines
  - `skills/session/CONTEXT.md` (+6/−6) — the shapes' homes: `session-log.ts` and `SKILL.md`'s sections
  - `skills/session/SKILL.md` (+64/−22) — intro points at `CONTEXT.md` § The session log for the words; step 1 in the pattern's shape; the merge gotcha carries the archive rule, the release gotcha the boundary sentence; § The plan part's status line (the table); `log` step 1 carries the four-row no-entry table and the vouching rule, step 2 the two Input/Output examples, step 4 the Narrative rule; § What is never rewritten closes the file
  - `skills/session/references/session-log.md` (+0/−176) — deleted; what the acts need moved into `SKILL.md`, the walk diagram (`/plan`'s), the session file template (`session template session` prints it) and § The words (`CONTEXT.md`) dropped
  - `skills/session/scripts/__tests__/paths.test.ts` (+0/−29) — folded into `session-log.test.ts`
  - `skills/session/scripts/__tests__/session-lib.test.ts` (+0/−176) — folded into `session-log.test.ts`
  - `skills/session/scripts/__tests__/session-log.test.ts` (+287/−0) — the merged tests, plus `render()` (heading and short sha, placeholders, a binary file, the release marker, the `MAX_FILES` cut, no files) and a test that `SKILL.md`'s entry template carries the skeleton's own lines
  - `skills/session/scripts/__tests__/session.test.ts` (+1/−1) — the comment names `session-log.ts`
  - `skills/session/scripts/__tests__/templates.test.ts` (+0/−44) — folded; its reference-pinning test replaced by the `SKILL.md` one
  - `skills/session/scripts/paths.ts` (+0/−47) — folded into `session-log.ts`
  - `skills/session/scripts/session-lib.ts` (+0/−170) — renamed and grown into `session-log.ts`
  - `skills/session/scripts/session-log.ts` (+410/−0) — the model: paths, the session file (header, template, placeholders, status), which session a run acts on, what a session accounts for, the entry skeleton (`Touched`, `Commit`, `MAX_FILES`, `render`), the three documents `init` writes
  - `skills/session/scripts/session.ts` (+18/−49) — imports from `session-log`; the types, `MAX_FILES`, `stat` and `render` moved out; the header comment says what it is
  - `skills/session/scripts/templates.ts` (+0/−140) — folded into `session-log.ts`
- Notes: Verified: `bun test` 27/0 (two files), `tsc`, `bun run validate`, plugin-kit's validator (structure clean, the same `evals/results/**` noise), description 1,003 characters, body 263 lines, grep for the old file names and `references/` over the repo returns only history; the gate here green. Left in `session.ts`'s header comment: nothing stale. The names: `session.ts` stays because the command is `session` and an executable named for its command is the convention; `session-log.ts` is the domain the module models, the glossary's own word, with no `-lib` suffix. Not measured yet: whether the folded body changes the disclosure figures — Part 5 Task 4's redo; one `--plugin-dir` render of `start` against this body ran after the commit (its result is in the Narrative).

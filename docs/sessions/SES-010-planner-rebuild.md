# 2026-08-31 14:50 · The planner rebuilt — PLAN-005 Part 3

- Goal: PLAN-005 Part 3 — planning-and-task-breakdown rebuilt as the one decomposition skill (ADR-006 D4): the union task grain (end-to-end Description, AC, Verification, Dependencies), both sizing rulers, tracer-bullet rules with prefactoring and the expand–contract exception, the quiz-the-user approval round, the freshness dial, and the Task List Target extended with the resolution order, per-ticket files and native blocking links; to-tickets to a pointer stub; § Continuing a plan byte-identical; the description untouched so cand3's measured rates hold by identity. Acceptance: the byte-identity diff, a /brain:plan headless brief unchanged in a scratch repo, validators green.
- Status: done
- Plan: PLAN-005 · part 3
- Outcome: PLAN-005 Part 3 done in one commit (`724a2bc`): the planner carries both lineages' decomposition doctrine behind its unchanged measured surfaces, to-tickets is a stub, and the daily door (`/brain:plan`) rendered its brief unchanged. Next: Part 4, the merged implement skill, in a fresh session.

## Narrative

One push (`724a2bc`). The design constraint drove everything: the two surfaces PLAN-001 Part 4 measured (the description, § Continuing a plan) must not move, so the union landed strictly as additions and both were verified unchanged rather than assumed — a section compare against HEAD for the walk, identity for the description. One stale-but-working pointer noted deliberately: § Continuing Step 8 still routes decisions to /brain:grill-with-docs, which is now a stub that resolves to interview-me; changing the byte-identical section is Part 8's routing-closure call, not this part's.

## Changes (one entry per commit, in order)

### 2026-08-31 · feat(planning-and-task-breakdown): the one decomposition skill — tracer-bullet rules with prefactoring, the expand–contract exception, the fresh-context-window second ruler, the freshness dial, the quiz-the-user approval round, and the Task List Target resolved by the issue-tracker order with per-ticket files and native blocking links; to-tickets retires to a stub; description untouched (cand3 rates hold by identity), § Continuing a plan byte-identical to HEAD, /brain:plan brief rendered unchanged in a scratch repo (PLAN-005 Part 3) · 724a2bc

- Summary: planning-and-task-breakdown is the one decomposition skill (PRD-002 requirement 4), the union landed as additions so its two measured surfaces stayed fixed: the description untouched (cand3's 10/10 · 13/13 hold by identity — to-tickets was typed-only, so no trigger claim is lost) and § Continuing a plan byte-identical (python section-compare vs HEAD). The additions: tracer-bullet rules with prefactor-first; the expand–contract exception for wide refactors; the fresh-context-window second ruler (smaller verdict wins); the freshness dial with AGENT-BRIEF durability rules and the prototype-snippet exception; Step 6 — the quiz-the-user approval round through brain:ask-user-question; the Task List Target resolved by the issue-tracker order with three renderings (tasks/todo.md, per-ticket files with Blocked-by and ready-for-agent, native tracker blocking links) and the frontier rule. The /brain:plan render in a scratch repo produced the full brief — every template line present, and the walk surfaced the fixture's planted missing-sha as a Finding.
- Why: PLAN-005 Part 3 Task 1 (ADR-006 D3/D4; ANA-009's S3 union; the corpus-seeded behavioral case).
- Files:
  - `skills/planning-and-task-breakdown/SKILL.md` (+18/−3) — the five union additions and the extended Task List Target
  - `skills/planning-and-task-breakdown/evals/evals.json` (+32/−0) — two behavioral cases (plan-of-parts execution; durable-tickets dialogue) with provenance
  - `skills/planning-and-task-breakdown/evals/fixtures/planning-and-task-breakdown/notifications-spec.md` (+18/−0) — the fixture, from the corpus
  - `skills/to-tickets/SKILL.md` (+2/−102) — retired to a pointer stub

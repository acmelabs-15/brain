# 2026-08-31 09:59 · The docs sweep and the duplicated shapes — PLAN-001 Part 5

- Goal: PLAN-001 Part 5 — the docs outside this repo delegate rehydration to `/brain:plan PLAN-NNN` (env-setup's root `CLAUDE.md` § Rehydrating, `README.md`, `OVERVIEW.md`, `docs/sessions/README.md`; `~/CLAUDE.md` §1), each duplicated shape gets one home (ADR template, spec template, ticket shape — Peter decides each), the references audit decides the two local references and the local-only pointers with him, and the session tool's nine subcommands are reviewed against the skill's three acts (ANA-004 from the research Peter named, then his decisions one at a time).
- Status: in progress
- Plan: PLAN-001 · part 5
- Outcome: _(fill in)_

## Narrative

Started 2026-08-31 right after SES-004 closed (Part 4 done: `/brain:plan` is the door, installed at `fe23f60`). Task 1 first — the delegation edits in env-setup go through env-setup's own ritual (a session there, as SES-008 was for Part 3 Task 4); `~/CLAUDE.md` is not a repo and is edited by hand. Tasks 2 and 3 are Peter's decisions shape by shape and reference by reference (`ask-user-question`, one at a time); Task 4 starts with the research he named (the two best-practices sections, plugin-kit's progressive-disclosure and pure-bun references, the skills overview, the wider web) written up as ANA-004 before any question is put to him.

Task 1, 2026-08-31: the grep found the bare `/plan PLAN-NNN` in env-setup's sessions README and
traced it to the tool's template — fixed at the source (`24327a7` here) and both repos'
READMEs regenerated with `init --refresh`, never hand-edited. env-setup's live text
(`CLAUDE.md` § Rehydrating retitled to "the way in", `README.md`, `OVERVIEW.md`'s table row and
How-to-resume) landed as `4fbddd4` there, on `feat/visual-grouping` (the repo sat on that branch
from the checkpoint's real work; the commit rides to `main` with SES-009's PR) — recorded by the
installed plugin's own `/brain:session-log SES-010` (`8ec333e`, which also swept
`CONTRIBUTING.md`'s "once it lands", a file my list had missed) and SES-010 closed (`1e75ca7`).
`~/CLAUDE.md` §1 edited by hand (not a repo), then reflowed so the acceptance grep sees the
phrase whole. `OVERVIEW.md`'s dated history bullets keep `/session start` as the name of their
day, the Part 3 Task 4 precedent. Task 4's research ran beside Task 1 (ANA-004, `868d15a`); the
decision rounds for Tasks 2, 3 and 4 are next, one question at a time.

## Changes (one entry per commit, in order)

### 2026-08-31 · fix(tool): the sessions-README template names /brain:plan PLAN-NNN — every typed form is namespaced (ADR-003); this repo's README regenerated with init --refresh · 24327a7

- Summary: The sessions-README template says `/brain:plan PLAN-NNN` where it said the bare `/plan PLAN-NNN`, and this repo's own `docs/sessions/README.md` is regenerated from it with `session init --refresh` — the index kept, the prose rewritten.
- Why: Part 5 Task 1's grep found the bare form in env-setup's sessions README; the prose there is the template's output, so a hand edit would have left the defect at its source and regrown it at the next refresh (ADR-003: every typed form is namespaced). Fixed at the cause; env-setup's copy is regenerated from this template in its own commit, recorded there (SES-010).
- Files:
  - `docs/sessions/README.md` (+2/−1) — regenerated by `init --refresh`: the prose line names `/brain:plan PLAN-NNN`; the index gains SES-005's row
  - `skills/session/scripts/core.ts` (+1/−1) — the template line in `sessionsReadme`
- Notes: Verified: `bun test` 29/0, `tsc`; the refresh diff touched only the one prose line and the index. Not done here: env-setup's copy (its ritual, SES-010 there); the installed plugin still carries the old template until the next reinstall — this fix rides Part 5's or Part 6's.

### 2026-08-31 · docs(analysis): ANA-004 — the session tool's surface against the three acts: what the best-practices page, the overview, the engineering post and plugin-kit's measured references say and do not say; the act↔subcommand map; the five decisions it tees up (PLAN-001 Part 5 Task 4's research half) · 868d15a

- Summary: ANA-004 records Task 4's research: the Template pattern covers output the model writes and the entry template already follows it; every source pushes deterministic scaffolding toward scripts; `--refresh` exists because shapes-in-code need a refresh path (env-setup `83d0881` the incident, the `/brain:plan` template fix its second use); the gate already is the page's validator loop and `validate` collides with the glossary's `_Avoid_` line; the act↔subcommand map from the shipped body names `current` and bare `list` as the two thin claims. Five decisions teed up for Peter, one at a time.
- Why: PLAN-001 Part 5 Task 4's research half — Peter's verbatim request names the two best-practices sections and the feedback-loops page as the starting point and asks for the research before any decision; the sources were read in full this conversation (best-practices 1,185 lines, the overview, the engineering post, plugin-kit's progressive-disclosure and pure-bun).
- Files:
  - `docs/analysis/ANA-004-session-tool-surface-against-the-acts.md` (+146/−0) — the analysis: question, sources, F1–F6, one refuted belief, the per-subcommand table, the five pending decisions
  - `docs/analysis/README.md` (+1/−0) — ANA-004's index row, status current — decisions pending
- Notes: Verified: every `session <subcommand>` mention in `SKILL.md` grepped and mapped (the table's "invoked by" column); the tool's `help` output read; `SKILL.md` measured at 275 lines. Unverified: whether any measured run used `current` or bare `list` — the transcripts predate the current surface (ANA-004 § Unverifiable). No decision is made in this commit; the AskUserQuestion round follows.

### 2026-08-31 · feat(skills): the ADR template has one home — documentation-and-adrs; domain-modeling/ADR-FORMAT.md points at it and keeps the three-test offer rule (PLAN-001 Part 5 Task 2, Peter's call) · df0205b

- Summary: The ADR template has one home, `documentation-and-adrs` § ADR Template: `ADR-FORMAT.md`'s minimal template block is replaced by a pointer paragraph naming the full shape and why; everything else in that file — the location deference, numbering, § When to offer an ADR with the three tests and the what-qualifies list — is untouched, because it is judgment guidance no other file carries, not a duplicated shape.
- Why: PLAN-001 Part 5 Task 2, shape 1 of 3, Peter's call by AskUserQuestion (option 1 — the full template, confirmed after a /wait-what walkthrough of what "points at it and keeps the three-test offer rule" means): every ADR in brain and env-setup follows the full shape, and the record's value has repeatedly been in the Alternatives section.
- Files:
  - `references/project-docs-conventions.md` (+2/−1) — the templates paragraph: the ADR template's one home since 2026-08-31; ADR-FORMAT.md points at it and keeps the when-to-offer rule
  - `skills/domain-modeling/ADR-FORMAT.md` (+7/−15) — the Template and Optional-sections blocks replaced by the pointer paragraph; location, numbering and § When to offer an ADR unchanged
- Notes: Verified: `grep ADR-FORMAT` — its citers (`domain-modeling/SKILL.md` line 74, the conventions reference, LOCAL-CHANGES as history) still resolve. Decision made on the facts as presented; immediately after confirming, Peter raised the wider concern — the Addy lifecycle likely defines the same concepts elsewhere, not fully aligned — which became PLAN-002 (next entry); shapes 2–3 of this task move there rather than being decided piecemeal.

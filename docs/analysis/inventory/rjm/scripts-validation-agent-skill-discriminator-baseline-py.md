---
package: rjm
path: scripts/validation/agent_skill_discriminator_baseline.py
type: script
bytes: 12142
unit: inv-rjm-272
in_scope_via: scripts/validation/check_agent_skill_discriminator.py
aliases: []
memo_inputs:
  - {path: scripts/validation/agent_skill_discriminator_baseline.py, sha256: 25e7f6b164c7be6361c22acacc683a54b4b053fb01051e8c0643d223f09dfef3}
method_sha: b35a07084cd4385d27423211bbcad6af860acbab8221e7e66315f877d3f517c7
template_sha: 3eead650a20bd7770bdfd54816e4316b9d5b017ed335d4138d8dd708f0c3eb23
model: Gemini 3.8 Flash
effort: high
verified: 2026-09-05 quote-check+coverage
---

# scripts/validation/agent_skill_discriminator_baseline.py

## Purpose — required, verbatim
> "Full-corpus baseline ratchet for the agent-skill discriminator (issue #4087)." — scripts/validation/agent_skill_discriminator_baseline.py:1

## Design intent — required
Provides baseline ratchet logic, Git state inspection, and dirty-input protection for the agent-skill discriminator (`scripts/validation/check_agent_skill_discriminator.py`). Separated from the primary discriminator module to comply with repository file size limits (500 lines per `.claude/rules/code-quality.md`), it allows pre-existing skill-shaped agents on `main` to remain without triggering false positives on unrelated PR edits, while ensuring that any regression above the recorded score threshold (0..3) fails the gate. By enforcing that full corpus scans query git tree state at `HEAD` rather than walking arbitrary untracked disk files, and by refusing baseline updates when scoring inputs differ from HEAD, it guarantees reproducible, tamper-resistant ratchet baselines across checkouts.

## Phase — required
none

## Inputs — required
- Arguments to module functions:
  - `repo_root` path (scripts/validation/agent_skill_discriminator_baseline.py:127)
  - `baseline` mapping (scripts/validation/agent_skill_discriminator_baseline.py:67)
  - `score` record (scripts/validation/agent_skill_discriminator_baseline.py:98)
  - `is_agent_path` callable predicate injected into `full_corpus_agent_paths` (scripts/validation/agent_skill_discriminator_baseline.py:149)
- Git repository tree at HEAD inspected via `git ls-tree` (scripts/validation/agent_skill_discriminator_baseline.py:139)
- Git working tree cleanliness status via `git diff` and `git ls-files` (scripts/validation/agent_skill_discriminator_baseline.py:197, 219)

## Outputs — required
- In-memory data structures:
  - `dict[str, int]` baseline mapping generated by `baseline_from_scores` — scripts/validation/agent_skill_discriminator_baseline.py:239
  - Tracked agent paths list or None returned by `full_corpus_agent_paths` — scripts/validation/agent_skill_discriminator_baseline.py:149
  - Boolean regression verdict from `is_regression` (scripts/validation/agent_skill_discriminator_baseline.py:98) and dirty-state check from `refuse_dirty_scoring_inputs` (scripts/validation/agent_skill_discriminator_baseline.py:183)
  - Formatted annotation string from `baseline_note` — scripts/validation/agent_skill_discriminator_baseline.py:252
- Standard error diagnostics when Git timeouts or dirty files are detected (scripts/validation/agent_skill_discriminator_baseline.py:199, 210, 222, 229)

## Invokes — required
- script portability_git — scripts/validation/agent_skill_discriminator_baseline.py:31
- doc code-quality.md — scripts/validation/agent_skill_discriminator_baseline.py:13
- doc ci-scripts.md — scripts/validation/agent_skill_discriminator_baseline.py:160

## Invoked by — required
- script check_agent_skill_discriminator.py — scripts/validation/agent_skill_discriminator_baseline.py:3

## Concepts named — required, verbatim
- `agent-skill discriminator` — scripts/validation/agent_skill_discriminator_baseline.py:1 — used here
- `ratchet` — scripts/validation/agent_skill_discriminator_baseline.py:14 — used here
- `DEFAULT_BASELINE_NAME` — scripts/validation/agent_skill_discriminator_baseline.py:37 — defined here
- `ADR-036` — scripts/validation/agent_skill_discriminator_baseline.py:39 — used here
- `AGENT_CORPUS_ROOTS` — scripts/validation/agent_skill_discriminator_baseline.py:41 — defined here
- `COMMAND_ROOTS` — scripts/validation/agent_skill_discriminator_baseline.py:46 — defined here
- `SCORING_ROOTS` — scripts/validation/agent_skill_discriminator_baseline.py:50 — defined here
- `MIN_BASELINE_SCORE` — scripts/validation/agent_skill_discriminator_baseline.py:63 — defined here
- `MAX_BASELINE_SCORE` — scripts/validation/agent_skill_discriminator_baseline.py:64 — defined here
- `MUST-9` — scripts/validation/agent_skill_discriminator_baseline.py:160 — used here

## Structure
- `validate_baseline_scores` — scripts/validation/agent_skill_discriminator_baseline.py:67
- `is_regression` — scripts/validation/agent_skill_discriminator_baseline.py:98
- `tracked_paths_at_head` — scripts/validation/agent_skill_discriminator_baseline.py:127
- `full_corpus_agent_paths` — scripts/validation/agent_skill_discriminator_baseline.py:149
- `refuse_dirty_scoring_inputs` — scripts/validation/agent_skill_discriminator_baseline.py:183
- `baseline_from_scores` — scripts/validation/agent_skill_discriminator_baseline.py:239
- `baseline_note` — scripts/validation/agent_skill_discriminator_baseline.py:252

## Scripts — required if type is script or the skill ships scripts
- path: `scripts/validation/agent_skill_discriminator_baseline.py`, language: Python, lines: 262
- documented invocation:
  - "the repo and records each one's score in a checked-in JSON file." — scripts/validation/agent_skill_discriminator_baseline.py:7
- executed: yes
- actual command run:
  `uv run python scripts/validation/agent_skill_discriminator_baseline.py`
  abridged stdout: `""` (no output; library module defining ratchet functions)
  actual exit code: 0
- documented exit codes vs actual exit paths:
  This file is a library module without a CLI `main()` entrypoint or direct `sys.exit()` calls. It raises `ValueError` on malformed score values (scripts/validation/agent_skill_discriminator_baseline.py:91) and returns boolean flags and status objects to caller `check_agent_skill_discriminator.py`.
- for validators/gates: can it exit non-zero? As a module, it raises `ValueError` in `validate_baseline_scores` when score is outside 0..3 (scripts/validation/agent_skill_discriminator_baseline.py:91), and returns boolean failure / `None` refusal flags to callers. Does it fail on the source repo's own default branch? No; 50 unit tests across `test_agent_skill_discriminator_baseline*.py` pass cleanly.
- does output match what documentation claims? Yes, helper routines correctly support baseline ratchet operations and git-backed corpus discovery.

## Defects — required
- missing-path · scripts/validation/agent_skill_discriminator_baseline.py:46 · `COMMAND_ROOTS` includes `"templates/commands/"` which does not exist on disk in the repository.

## Observations
Split out from `check_agent_skill_discriminator.py` to stay under the 500-line ceiling mandated by `.claude/rules/code-quality.md`. It avoids circular imports with `check_agent_skill_discriminator.py` by importing `AgentScore` only under `TYPE_CHECKING` (line 29) and taking `is_agent_path` as an injected callable parameter. It incorporates a dirty-state guard that fails closed if git diff or untracked file listings under scoring roots indicate working tree modifications.

## Context cost
File size: 12142 bytes, 262 lines, ~3000 tokens. When loaded with its dependency `scripts/validation/portability_git.py` (15589 bytes), total is ~27731 bytes (~6900 tokens).

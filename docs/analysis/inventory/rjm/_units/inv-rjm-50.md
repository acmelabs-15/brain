---
unit: inv-rjm-50
phase: 1
package: rjm
session: 004
subagent_returned: complete
---

# Unit inv-rjm-50

## Files assigned
- [x] `/Users/peterkloss/Dev/ACMElabs/brain-v2/sources/rjm/.agents/architecture/DESIGN-REVIEW-template.md`
- [x] `/Users/peterkloss/Dev/ACMElabs/brain-v2/sources/rjm/.agents/architecture/README.md`

## Outputs produced
- `docs/analysis/inventory/rjm/agents-architecture-design-review-template-md.md` (8079 bytes)
- `docs/analysis/inventory/rjm/agents-architecture-readme-md.md` (13686 bytes)

## Scripts executed
- `.agents/architecture/README.md` inline snippet: `uv run --directory sources/rjm python -c "import pathlib, re, yaml; _CLOSING_FENCE = re.compile(r'\r?\n---\r?\n'); [print(front.get('id') or p.name) for p in sorted(pathlib.Path('.agents/architecture').glob('ADR-[0-9]*.md')) if p.read_text(encoding='utf-8').startswith('---') and (closing := _CLOSING_FENCE.search(p.read_text(encoding='utf-8'), 3)) and str((yaml.safe_load(p.read_text(encoding='utf-8')[3:closing.start()]) or {}).get('status', '')).strip().lower() == 'accepted']"`, exit code 0
- `build/scripts/generate_adr_index.py`: `uv run --directory sources/rjm python build/scripts/generate_adr_index.py --check`, exit code 0
- `build/scripts/build_all.py`: `uv run --directory sources/rjm python build/scripts/build_all.py --help`, exit code 0
- `scripts/validation/check_adr_lifecycle.py`: `uv run --directory sources/rjm python scripts/validation/check_adr_lifecycle.py`, exit code 0

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- Unit inv-rjm-50 covers the central architecture index (`.agents/architecture/README.md`) and the architect review template (`.agents/architecture/DESIGN-REVIEW-template.md`) in `rjm`.
- `.agents/architecture/README.md` is the navigation hub and entry point for all 104 ADRs in the repository. It is generated by `build/scripts/generate_adr_index.py` (orchestrated by `build/scripts/build_all.py`) and verified by `scripts/validation/check_adr_lifecycle.py`. Running `generate_adr_index.py --check` confirms that the committed README.md matches the 104 ADR records with 0 diff.
- `.agents/architecture/DESIGN-REVIEW-template.md` implements the template mandated by `ADR-051` (Synthesis Panel Frontmatter Standard). It standardizes architect agent synthesis reviews into structured YAML frontmatter (`status`, `priority`, `reviewer`, `date`) and defines CI gate rules for PR merge blocking.
- A defect was identified in `DESIGN-REVIEW-template.md`: line 4 introduces a `blocking` frontmatter field with a comment citing `NEEDS_ADR` and `REJECTED` status values, which contradicts the valid status enums (`APPROVED`, `NEEDS_CHANGES`, `BLOCKED`) defined in the same file and in `ADR-051`, and is omitted from the template's own Frontmatter Reference table.
- None of this unit's files appear in `docs/analysis/manifest/rjm-duplicates.md` (no ledger duplicates; no divergence cards needed).

## Blocked or uncertain
none

## Time and size
Approximate tokens of source read: ~7,915 tokens (31,666 bytes across 2 files); approximate tokens of output written: ~5,440 tokens (21,765 bytes across 2 inventory cards).

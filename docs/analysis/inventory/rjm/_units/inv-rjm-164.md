---
unit: inv-rjm-164
phase: 1
package: rjm
session: 006
subagent_returned: complete
---

# Unit inv-rjm-164

## Files assigned
- [x] /Users/peterkloss/Dev/ACMElabs/brain-v2/sources/rjm/.claude/skills/skillforge/scripts/triage_skill_request.py

## Outputs produced
- docs/analysis/inventory/rjm/claude-skills-skillforge-scripts-triage-skill-request-py.md (15881 bytes)
- docs/analysis/inventory/rjm/_units/inv-rjm-164.md

## Scripts executed
- `.claude/skills/skillforge/scripts/triage_skill_request.py`, `python3 sources/rjm/.claude/skills/skillforge/scripts/triage_skill_request.py "create a skill for code review"`, exit code 2 (missing index file in ~/.cache/skillrecommender/)
- `.claude/skills/skillforge/scripts/discover_skills.py`, `python3 sources/rjm/.claude/skills/skillforge/scripts/discover_skills.py`, exit code 0 (populated index at ~/.cache/skillrecommender/skill_index.json)
- `.claude/skills/skillforge/scripts/triage_skill_request.py`, `python3 sources/rjm/.claude/skills/skillforge/scripts/triage_skill_request.py "create a skill for code review"`, exit code 0 (returned CREATE_NEW)
- `.claude/skills/skillforge/scripts/triage_skill_request.py`, `python3 sources/rjm/.claude/skills/skillforge/scripts/triage_skill_request.py "help me debug this error" --json`, exit code 0 (returned CLARIFY / general category)
- `.claude/skills/skillforge/scripts/triage_skill_request.py`, `python3 sources/rjm/.claude/skills/skillforge/scripts/triage_skill_request.py "TypeError: Cannot read property 'map'"`, exit code 0 (returned CREATE_NEW)

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- Unit `inv-rjm-164` covers `.claude/skills/skillforge` part 7 (`scripts/triage_skill_request.py`, 29,094 bytes, 794 lines). Sibling units cover parts 1–6 (references, other scripts like `discover_skills.py`, `validate-skill.py`, `init_skill.py`, etc., and `SKILL.md`).
- Context and skill-level integration:
  - `triage_skill_request.py` implements the Phase 0 triage router documented in `.claude/skills/skillforge/SKILL.md:120` and `.claude/skills/skillforge/references/phase0-triage.md`.
  - It expects an index generated by sibling script `discover_skills.py` at `~/.cache/skillrecommender/skill_index.json` (`.claude/skills/skillforge/scripts/discover_skills.py:106` notes that domain names MUST match `DOMAIN_SYNONYMS` in `triage_skill_request.py`).
- Key defects observed:
  - `doc-drift`: Exit code 1 ("1 - General failure") is documented in line 22, but line 788's `else 1` is unreachable dead code because `triage_request()` only ever returns `success=False` on missing index, triggering exit code 2.
  - `script-bug`: `TASK_REQUEST_PATTERNS` regex strictly expects "with" or "to" after "help me", causing the documented example `"help me debug this error"` to fall through to `InputCategory.GENERAL`.
  - `missing-path`: Index path `~/.cache/skillrecommender/skill_index.json` is outside the repository workspace and requires running `discover_skills.py` first.
- Duplication ledger:
  - Neither `triage_skill_request.py` nor any variant appears in `docs/analysis/manifest/rjm-duplicates.md`. No divergence card is required.

## Blocked or uncertain
none

## Time and size
- Approximate tokens of source read: ~7,300 tokens (29,094 bytes for `triage_skill_request.py`) plus ~4,000 tokens for context read (`SKILL.md` and `phase0-triage.md`).
- Approximate tokens of output written: ~3,900 tokens (inventory card + unit report).

---
unit: inv-matt-30
phase: 1
package: matt
session: 003
subagent_returned: complete
---

# Unit inv-matt-30

## Files assigned
- [x] `/Users/peterkloss/Dev/ACMElabs/brain-v2/sources/matt-external/triage.md`

## Outputs produced
- `docs/analysis/inventory/matt/external-triage-md.md` (12375 bytes)

## Scripts executed
none

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- Unit inv-matt-30 covers `external/triage.md` (376,711 bytes, 74 lines), the external HTML documentation snapshot for `/triage` hosted at `https://aihero.dev/skills-triage`.
- Architectural relationships with other skills in Matt's package:
  - `to-tickets`: Sits in the parallel internally-originated chain. The external doc clarifies that `to-tickets` outputs tickets pre-labeled `ready-for-agent` by construction, so running `triage` on internally planned tickets is redundant and wasted work.
  - `diagnosing-bugs`: Sits downstream or alongside `triage`. Step 3 in `triage` performs a shallow verification (a few minutes to confirm or move to `needs-info`), routing deeper root-cause debugging to `diagnosing-bugs`. The doc explicitly notes that neither skill's text references the other, leaving the seam open.
  - `grilling` and `domain-modeling`: Invoked by `triage` when an incoming request needs sharpening before an agent brief can be attached, updating `CONTEXT.md` and ADRs inline.
  - `setup-matt-pocock-skills`: Provisions tracker configuration templates (`issue-tracker-*.md`) and writes label vocabulary to `docs/agents/triage-labels.md`.
  - `implement`: Downstream consumer of `ready-for-agent` briefs generated by `triage`.
  - `ask-matt`: Routes users between the inbound lane (`triage`) and the internal authoring lane (`grill-with-docs`).
- Defects and open issues documented on the page:
  - Issue #468: External-PR listing command in `setup-matt-pocock-skills/issue-tracker-github.md:23` requests `authorAssociation` from `gh pr list`, which fails outright.
  - Issue #616: `setup-matt-pocock-skills` writes label vocabulary to `docs/agents/triage-labels.md` but does not run `gh label create`, causing failures when applying labels that do not already exist on the tracker.
  - Issue #200: Local-markdown tracker template duplicate acceptance criteria bug.
  - Issues #139, #297: Community demand for additional state roles (`blocked`/`paused`, `deferred`, `implemented`) beyond the canonical 5 states.
- Duplication ledger status: None of this unit's files appear in `docs/analysis/manifest/matt-duplicates.md` (no ledger duplicates; no divergence card needed).

## Blocked or uncertain
none

## Time and size
Approximate tokens of source read: ~94,200 tokens (376,711 bytes raw HTML snapshot; ~3,500 words / ~4,500 tokens core article text); approximate tokens of output written: ~3,800 tokens across inventory card (12,375 bytes) and unit report.

---
package: matt
path: package-lock.json
type: config
bytes: 49290
unit: inv-matt-36
deprecated: false
aliases: []
memo_inputs:
  - {path: package-lock.json, sha256: b89967d44b654ceb7b9fcc7e0d6c3f74fb110bb96eb66c65bac14099c729338d}
method_sha: b35a07084cd4385d27423211bbcad6af860acbab8221e7e66315f877d3f517c7
template_sha: 3eead650a20bd7770bdfd54816e4316b9d5b017ed335d4138d8dd708f0c3eb23
model: Gemini 3.8 Flash
effort: high
verified: 2026-09-05 quote-check+coverage
---

# package-lock.json

## Purpose — required, verbatim
> "mattpocock-skills" — package-lock.json:2
(no explicit purpose statement)

## Design intent — required
Npm lockfile version 3 (`lockfileVersion: 3`) providing deterministic dependency resolution for the `mattpocock-skills` repository. It locks the exact versions, integrity hashes (SHA-512), resolved tarball URLs, and dependency trees for `@changesets/cli`, `@changesets/changelog-github`, and all their transitive dependencies. Without this file, installs via `npm ci` or `npm install` could resolve different minor or patch versions over time, introducing instability or breaking Changeset versioning workflows.

## Phase — required
none

## Inputs — required
`package.json` dependencies specification and npm registry package resolution metadata.

## Outputs — required
Deterministic dependency tree written to `node_modules/` during `npm ci` or `npm install`.

## Invokes — required
none

## Invoked by — required
orphan

## Concepts named — required, verbatim
- `mattpocock-skills` — package-lock.json:2 — defined here
- `lockfileVersion` — package-lock.json:4 — defined here
- `requires` — package-lock.json:5 — defined here
- `@changesets/changelog-github` — package-lock.json:12 — used here
- `@changesets/cli` — package-lock.json:13 — used here
- `node_modules` — package-lock.json:16 — defined here
- `@changesets/apply-release-plan` — package-lock.json:26 — used here
- `@changesets/assemble-release-plan` — package-lock.json:48 — used here
- `changeset` — package-lock.json:120 — defined here
- `package-manager-detector` — package-lock.json:982 — used here

## Structure
none

## Scripts — required if type is script or the skill ships scripts
none

## Defects — required
- cross-file-contradiction — "0.0.0" — package-lock.json:3 — root package version in lockfile is "0.0.0", contradicting package.json which specifies version 1.2.3.

## Observations
Generated by `npm@10.9.4` with `lockfileVersion: 3`. It captures 1,386 lines of dependency metadata, pinning the exact versions and integrity hashes of the Changesets toolchain and its transitive closure (e.g. Babel runtime, Prettier, micromatch, fast-glob, enquirer). Notably, the root package version remains `0.0.0` (lines 3 and 9) while the release process advances `package.json` to `1.2.3` via `npm run version` without running `npm install` to update the lockfile.

## Context cost
49290 bytes, approximately 12,000 tokens.

---
package: matt
path: package.json
type: config
bytes: 597
unit: inv-matt-36
deprecated: false
aliases: []
memo_inputs:
  - {path: package.json, sha256: 167010d1c535a085be165835a4cc89e1de2760c2bf3b6e43c6ae045e41d0587b}
method_sha: b35a07084cd4385d27423211bbcad6af860acbab8221e7e66315f877d3f517c7
template_sha: 3eead650a20bd7770bdfd54816e4316b9d5b017ed335d4138d8dd708f0c3eb23
model: Gemini 3.8 Flash
effort: high
verified: 2026-09-05 quote-check+coverage
---

# package.json

## Purpose — required, verbatim
> "Matt Pocock's agent skills for real engineering" — package.json:5

## Design intent — required
Root package manifest for `mattpocock-skills`. Defines repository metadata, establishes the package as private (`"private": true`), pins the package manager to `npm@10.9.4`, and declares development dependencies for Changeset versioning (`@changesets/cli` and `@changesets/changelog-github`). It also defines npm scripts (`changeset`, `version`, `check-plugin-version`) that automate release preparation and synchronize the npm package version with `.claude-plugin/plugin.json` via `scripts/sync-plugin-version.mjs`.

## Phase — required
none

## Inputs — required
Changeset definitions in `.changeset/`, version synchronization script `scripts/sync-plugin-version.mjs`, and `.claude-plugin/plugin.json`.

## Outputs — required
Version bumps in `package.json`, release changelogs generated by `@changesets/cli`, and synchronized versioning in `.claude-plugin/plugin.json`.

## Invokes — required
- script scripts/sync-plugin-version.mjs — package.json:13

## Invoked by — required
- script scripts/sync-plugin-version.mjs — scripts/sync-plugin-version.mjs:13

## Concepts named — required, verbatim
- `mattpocock-skills` — package.json:2 — defined here
- `version` — package.json:3 — defined here
- `private` — package.json:4 — defined here
- `description` — package.json:5 — defined here
- `license` — package.json:10 — defined here
- `scripts` — package.json:11 — defined here
- `changeset` — package.json:12 — defined here
- `check-plugin-version` — package.json:14 — defined here
- `devDependencies` — package.json:16 — defined here
- `@changesets/changelog-github` — package.json:17 — used here
- `@changesets/cli` — package.json:18 — used here
- `packageManager` — package.json:20 — defined here

## Structure
none

## Scripts — required if type is script or the skill ships scripts
none

## Defects — required
none

## Observations
`package.json` configures a minimal workspace with only two devDependencies (`@changesets/changelog-github` and `@changesets/cli`). It bridges npm-based tooling with Claude Code plugin structure by chaining `changeset version && node scripts/sync-plugin-version.mjs` in the `version` script, ensuring that `.claude-plugin/plugin.json` always reflects the current release version.

## Context cost
597 bytes, approximately 130 tokens.

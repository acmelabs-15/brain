---
unit: inv-addy-30
phase: 1
package: addy
session: 008
subagent_returned: complete
---

# Unit inv-addy-30

## Files assigned
- [x] `sources/addy-external/code-review-and-quality.md` (26,687 bytes)

## Outputs produced
- `docs/analysis/inventory/addy/external-code-review-and-quality-md.md` (23,267 bytes)
- `docs/analysis/inventory/addy/_units/inv-addy-30.md` (5,120 bytes)

## Scripts executed
- External Vercel Skills CLI (`@vercel-labs/skills`) — `bunx --yes skills add addyosmani/agent-skills --skill code-review-and-quality -y --copy` (in isolated temp directory) — Exit code: `0` (Output: cloned repository, selected `code-review-and-quality`, and copied `SKILL.md` (20,555 bytes) to `.agents/skills/code-review-and-quality/SKILL.md`)
- External Vercel Skills CLI (`@vercel-labs/skills`) — `bunx --yes skills add addyosmani/agent-skills -l` — Exit code: `0` (Output: enumerated all 25 skills in `addyosmani/agent-skills` including `code-review-and-quality`)
- Upstream Skill Validation — `cd sources/addy && bun scripts/validate-skills.js` — Exit code: `0` (Output: 25 skills checked — 0 error(s), 0 warning(s) — PASSED; validates `code-review-and-quality`)
- Upstream Reference Link Validation — `cd sources/addy && bun scripts/validate-reference-links.js` — Exit code: `0` (Output: 25 skills checked — 0 error(s) — PASSED; validates `skills/code-review-and-quality/SKILL.md`)
- Upstream Command Parity & Description Validation — `cd sources/addy && bun scripts/validate-commands.js` — Exit code: `0` (Output: 9 commands checked — 0 error(s) — PASSED; validates `/review`, `/code-simplify`, `/webperf`)
- Upstream Plugin Manifest Version Validation — `cd sources/addy && bun scripts/validate-versions.js` — Exit code: `0` (Output: All plugin manifests use version 0.6.8)
- Upstream Artifact Paths Validation — `cd sources/addy && bun scripts/validate-artifact-paths.js` — Exit code: `0` (Output: 7 files checked — 0 error(s) — PASSED)
- Upstream Evals Suite — `cd sources/addy && bun scripts/run-evals.js` — Exit code: `0` (Output: 136 checks passed — 0 error(s), 0 warning(s) across 25 skills)
- Upstream Behavioral Eval Dry Run — `cd sources/addy && bun scripts/run-evals.js --behavioral code-review-and-quality --dry-run` — Exit code: `0` (Output: [dry-run] eval 1: execution trace in workspace + 1 fixture(s))
- Synthesis Glossary Lint — `bun run scripts/synthesis/glossary-lint.ts` — Exit code: `0` (Output: Glossary lint: clean)
- Synthesis Coverage Check — `bun run scripts/synthesis/coverage.ts` — Exit code: `1` (Output: Unchecked manifest rows: 1202, Empty required inventory fields: 0)

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- **External Documentation Architectural Role:**
  - `sources/addy-external/code-review-and-quality.md` is a rendered HTML snapshot generated by Astro for the public web catalog at `https://skills.addy.ie/skills/code-review-and-quality/`.
  - It functions as a public discovery, cataloging, command mapping, and distribution landing page rather than an executable agent skill file.
- **Canonical Implementation Precedence (Companion to inv-addy-16):**
  - The external page displays a standardized 6-section anatomy overview (`01 Overview`, `02 When to Use`, `03 Process`, `04 Rationalizations`, `05 Red Flags`, `06 Verification`) and links directly to GitHub (`https://github.com/addyosmani/agent-skills/blob/main/skills/code-review-and-quality/SKILL.md`) for the full operational workflow.
  - `SKILL.md` (inventoried in unit `inv-addy-16` as `skills-code-review-and-quality-skill-md.md`) remains the authoritative source of procedural instructions and contains granular operational patterns (The Approval Standard, Five-Axis Review, Structural Remedies, Change Sizing ~100 lines vs ~1000 lines, Standalone Change Descriptions, 5-Step Review Workflow, Severity Labeling, Multi-Model Review Pattern, Dead Code Hygiene, Review Speed Norms, Disagreement Hierarchy, Honesty Standards, Dependency Discipline, and Presumptive Blockers) that the public landing page condenses or omits.
- **Defects Cataloged (`doc-drift`, `missing-path`, `orphan`):**
  - *Purpose & Description Drift:* External page highlights change sizing (~100 lines), severity labels (Critical/Required/Optional/Nit), review speed norms, and splitting strategies (`lines 1, 5`), condensing `SKILL.md:10` and omitting the core Approval Standard philosophy (`SKILL.md:12`).
  - *Trigger Condition Condensation:* External page reduces triggering conditions to a single line `"Before merging any change."` (`line 5`), omitting two additional trigger scenarios documented in `SKILL.md:3`.
  - *Structural Anatomy Drift:* External page claims every skill follows a 6-part anatomy including `03 Process — Step-by-step workflow`, but `SKILL.md` has no section titled `Process`, organizing its workflow under `## The Five-Axis Review` and `## Review Workflow & Communication`.
  - *Severity Label 4-Way Inconsistency:* External page documents `(Critical/Required/Optional/Nit)` (`line 1, 5`); `README.md:268` states `(Nit/Optional/FYI)`; command files (`commands/review.toml:14`, `.claude/commands/review.md:15`) state `Critical, Important, or Suggestion`; and `SKILL.md:180-188` defines `Critical:`, `(no prefix)` (Required), `Nit:`, `Optional: / Consider:`, and `FYI`.
  - *Command Binding Omission in Skill Body:* External page explicitly pairs `code-review-and-quality` with `/review` and `Review phase` (`lines 5, 12`), whereas `SKILL.md` itself omits slash command references, relying entirely on repository root configuration files (`commands/review.toml`, `CLAUDE.md:25`, `README.md:367`).
  - *Installation Tooling:* External page documents installation via `npx skills add addyosmani/agent-skills --skill code-review-and-quality` using Vercel Labs' open `skills` CLI, which is not mentioned in repository root files (`README.md`, `CLAUDE.md`, `AGENTS.md`).
  - *Format Mismatch:* The file bears a `.md` markdown extension in `sources/addy-external/` and the manifest, but contains 100% compiled HTML (`<!DOCTYPE html><html...`).
  - *Missing Internal Paths:* Web-relative paths (`/skills/`, `/docs/getting-started/`, `/tutorials/`, `/lifecycle/`, `/loops/`, `/teach/`, `/compare/`, `/_astro/...`, `/.netlify/...`) do not resolve locally on disk.
  - *Orphan Status:* Brought into scope exclusively via METHOD.md §1.1; not linked or invoked by any source file in `sources/addy/`.
- **Review Phase Cluster:**
  - The external landing page explicitly cross-references `code-simplification` (`/code-simplify`), `security-and-hardening`, and `performance-optimization` (`/webperf`) under "More in the Review phase", validating the clustering of Review-stage skills.
  - `security-and-hardening` is the only Review-phase skill without a slash command badge, matching repository command parity where no `/security` command exists.
- **Session Assignment:**
  - `STATE.md:94` and current session progression assign unit `inv-addy-30` to session `008` batch execution.

## Blocked or uncertain
none

## Time and size
- Approximate tokens of source read: ~7,500 tokens (26,687 bytes of external HTML snapshot plus cross-referenced skill and command files).
- Approximate tokens of output written: ~5,500 tokens across 1 inventory entry and 1 unit report.

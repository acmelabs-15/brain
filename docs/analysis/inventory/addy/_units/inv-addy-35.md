---
unit: inv-addy-35
phase: 1
package: addy
session: 009
subagent_returned: complete
---

# Unit inv-addy-35

## Files assigned
- [x] `sources/addy-external/code-simplification.md` (26,320 bytes)

## Outputs produced
- `docs/analysis/inventory/addy/external-code-simplification-md.md` (21,953 bytes)
- `docs/analysis/inventory/addy/_units/inv-addy-35.md` (5,120 bytes)

## Scripts executed
- External Vercel Skills CLI (`@vercel-labs/skills`) — `bunx --yes skills add addyosmani/agent-skills --skill code-simplification -y --copy` (in isolated temp directory) — Exit code: `0` (Output: cloned repository, selected `code-simplification`, and copied `SKILL.md` (13,545 bytes) to `.agents/skills/code-simplification/SKILL.md`)
- External Vercel Skills CLI (`@vercel-labs/skills`) — `bunx --yes skills add addyosmani/agent-skills -l` — Exit code: `0` (Output: enumerated all 25 skills in `addyosmani/agent-skills` including `code-simplification`)
- Upstream Skill Validation — `cd sources/addy && bun scripts/validate-skills.js` — Exit code: `0` (Output: 25 skills checked — 0 error(s), 0 warning(s) — PASSED; validates `code-simplification`)
- Upstream Reference Link Validation — `cd sources/addy && bun scripts/validate-reference-links.js` — Exit code: `0` (Output: 25 skills checked — 0 error(s) — PASSED; validates `skills/code-simplification/SKILL.md`)
- Upstream Command Parity & Description Validation — `cd sources/addy && bun scripts/validate-commands.js` — Exit code: `0` (Output: 9 commands checked — 0 error(s) — PASSED; validates `/code-simplify`)
- Upstream Plugin Manifest Version Validation — `cd sources/addy && bun scripts/validate-versions.js` — Exit code: `0` (Output: All plugin manifests use version 0.6.8)
- Upstream Artifact Paths Validation — `cd sources/addy && bun scripts/validate-artifact-paths.js` — Exit code: `0` (Output: 7 files checked — 0 error(s) — PASSED)
- Upstream Evals Suite — `cd sources/addy && bun scripts/run-evals.js` — Exit code: `0` (Output: 136 checks passed — 0 error(s), 0 warning(s) across 25 skills)
- Upstream Behavioral Eval Dry Run — `cd sources/addy && bun scripts/run-evals.js --behavioral code-simplification --dry-run` — Exit code: `0` (Output: [dry-run] eval 1: execution trace in workspace + 1 fixture(s))
- Upstream Companion Hook Unit Tests — `cd sources/addy && bash hooks/simplify-ignore-test.sh` — Exit code: `0` (Output: Results: 21 passed, 0 failed)
- Synthesis Glossary Lint — `bun run scripts/synthesis/glossary-lint.ts` — Exit code: `0` (Output: Glossary lint: clean)
- Synthesis Coverage Check — `bun run scripts/synthesis/coverage.ts` — Exit code: `1` (Output: Unchecked manifest rows: 1192, Empty required inventory fields: 0)

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- **External Documentation Architectural Role:**
  - `sources/addy-external/code-simplification.md` is a rendered HTML snapshot generated by Astro for the public web catalog at `https://skills.addy.ie/skills/code-simplification/`.
  - It functions as a public discovery, cataloging, command mapping, and distribution landing page rather than an executable agent skill prompt.
- **Canonical Implementation Precedence (Companion to inv-addy-18):**
  - The external page displays a standardized 6-section anatomy overview (`01 Overview`, `02 When to Use`, `03 Process`, `04 Rationalizations`, `05 Red Flags`, `06 Verification`) and links directly to GitHub (`https://github.com/addyosmani/agent-skills/blob/main/skills/code-simplification/SKILL.md`) for the full operational workflow.
  - `SKILL.md` (inventoried in unit `inv-addy-18` as `skills-code-simplification-skill-md.md`) remains the authoritative source of procedural instructions, containing The Five Principles, The Simplification Process (4 steps), Language-Specific Guidance (TypeScript, Python, React/JSX), Rationalizations to Reject, Red Flags, and the Verification Checklist that the public landing page summarizes.
- **Defects Cataloged (`doc-drift`, `missing-path`, `orphan`, `other`):**
  - *Sidebar Command Mismatch (Major):* External page sidebar card for Review phase hardcodes `Command: /review` (`line 12`), which conflicts with the top header command badge `/code-simplify` (`line 5`), the repository command file `commands/code-simplify.toml`, and the skill mapping. In `skills.addy.ie` web templates, the sidebar erroneously hardcoded `/review` instead of the skill's dedicated command `/code-simplify`.
  - *Structural Anatomy Drift:* External page claims every skill follows a 6-part anatomy including `03 Process — Step-by-step workflow`, but `SKILL.md` organizes its workflow under `## The Five Principles`, `## The Simplification Process` (4 steps), and `## Language-Specific Guidance`.
  - *Triggering Condition Condensation:* External page reduces triggering conditions to a single line `"Code works but is harder to read or maintain than it should be."` (`line 5`), omitting detailed positive and negative triggers from `SKILL.md:14–29`.
  - *Missing Internal Paths:* Web-relative paths (`/skills/`, `/docs/getting-started/`, `/tutorials/`, `/lifecycle/`, `/loops/`, `/teach/`, `/compare/`, `/_astro/...`, `/.netlify/...`) do not resolve locally on disk.
  - *Orphan Status:* Brought into scope exclusively via METHOD.md §1.1; not linked or invoked by any source file in `sources/addy/`.
  - *Format Mismatch:* The file carries a `.md` markdown extension in `sources/addy-external/` and the manifest, but contains 100% compiled HTML (`<!DOCTYPE html><html...`).
  - *Installation Tooling:* External page documents installation via `npx skills add addyosmani/agent-skills --skill code-simplification` using Vercel Labs' open `skills` CLI, which is not mentioned in repository root files (`README.md`, `CLAUDE.md`, `AGENTS.md`).
- **Review Phase Cluster:**
  - The external landing page explicitly cross-references `code-review-and-quality` (`/review`), `security-and-hardening`, and `performance-optimization` (`/webperf`) under "More in the Review phase", validating the Review-stage skill cluster.
- **Session Assignment:**
  - `STATE.md:99` assigns unit `inv-addy-35` to session `009`.

## Blocked or uncertain
none

## Time and size
- Approximate tokens of source read: ~7,200 tokens (26,320 bytes of external HTML snapshot plus cross-referenced skill, command, and hook files).
- Approximate tokens of output written: ~5,500 tokens across 1 inventory entry and 1 unit report.

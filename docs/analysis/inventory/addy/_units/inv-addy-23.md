---
unit: inv-addy-23
phase: 1
package: addy
session: 007
subagent_returned: complete
---

# Unit inv-addy-23

## Files assigned
- [x] `sources/addy-external/planning-and-task-breakdown.md` (22,310 bytes)
- [x] `sources/addy-external/incremental-implementation.md` (26,461 bytes)

## Outputs produced
- `docs/analysis/inventory/addy/external-planning-and-task-breakdown-md.md` (17,569 bytes)
- `docs/analysis/inventory/addy/external-incremental-implementation-md.md` (21,169 bytes)
- `docs/analysis/inventory/addy/_units/inv-addy-23.md` (4,554 bytes)

## Scripts executed
- External Vercel Skills CLI (`@vercel-labs/skills`) — `npx --yes skills add addyosmani/agent-skills --list` — Exit code: `0` (Output: resolved remote package and listed `planning-and-task-breakdown` with description and `--skill` flag guidance)
- External Vercel Skills CLI (`@vercel-labs/skills`) — `npx --yes skills add addyosmani/agent-skills --skill incremental-implementation --list` — Exit code: `0` (Output: resolved remote package and confirmed `incremental-implementation` with description and `--skill` flag guidance)

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- **External Documentation Architectural Role:** Both assigned files in `sources/addy-external/` are rendered HTML snapshots generated by Astro for the public web catalog at `https://skills.addy.ie`. They serve as public discovery, marketing, command mapping, and distribution landing pages rather than executable agent instructions.
- **Canonical Implementation Precedence:** The external pages display a standardized 6-section anatomy overview (`Overview`, `When to Use`, `Process`, `Rationalizations`, `Red Flags`, `Verification`) and link directly to GitHub (`https://github.com/addyosmani/agent-skills/blob/main/skills/<name>/SKILL.md`) for the full operational workflow. `SKILL.md` remains the authoritative source of procedural instructions and contains all granular guardrails (e.g. task sizing XS-XL, plan overwrite protection, slicing rules, and checklists) that the public web pages omit.
- **Defects Cataloged (`doc-drift`):**
  - *Summary Drift:* Both external docs diverge in their summary copy from `SKILL.md:3`. External `planning-and-task-breakdown` adds "with acceptance criteria and dependency ordering"; external `incremental-implementation` adds "as thin vertical slices with feature flags, safe defaults, and rollback-friendly changes".
  - *Trigger Simplification:* Both external pages compress comprehensive positive/negative trigger conditions from `SKILL.md` into a single short sentence, omitting "When NOT to use" guidance.
  - *Installation Tooling:* The external docs instruct installation via `npx skills add addyosmani/agent-skills --skill <name>` using Vercel Labs' open `skills` CLI (`github.com/vercel-labs/skills`), an installation mechanism completely absent from in-repository documentation.
  - *Format Mismatch:* The files bear `.md` extensions in `sources/addy-external/` and the manifest, but contain 100% compiled HTML (`<!DOCTYPE html><html...`).
- **Phase Allocation & Command Bindings:**
  - `planning-and-task-breakdown.md` is bound to `addy:Plan` and command `/plan`.
  - `incremental-implementation.md` is bound to `addy:Build` and command `/build`.
- **Phase Cross-References Asymmetry:** `incremental-implementation.md` provides a "More in the Build phase" card grid cross-referencing `test-driven-development` (`/test`), `context-engineering`, and `source-driven-development`. `planning-and-task-breakdown.md` lacks any sibling links.
- **Evaluation Linkage:**
  - `evals/cases/planning-and-task-breakdown.json` validates plan generation against `notifications-spec.md`.
  - `evals/cases/incremental-implementation.json` validates incremental slicing (`reports.js`, `reports.test.js`, `tasks/plan.md`) and pressure-testing against sunk-cost rationalization (`draft-export.js`, `scenario.md`).

## Blocked or uncertain
none

## Time and size
- Approximate tokens of source read: ~12,200 tokens (48,771 bytes of external HTML snapshots plus corresponding internal skill and evaluation references).
- Approximate tokens of output written: ~10,500 tokens across 2 inventory entries and 1 unit report.

---
unit: inv-addy-31
phase: 1
package: addy
session: 009
subagent_returned: complete
---

# Unit inv-addy-31

## Files assigned
- [x] `sources/addy-external/security-and-hardening.md` (26,475 bytes)

## Outputs produced
- `docs/analysis/inventory/addy/external-security-and-hardening-md.md` (26,928 bytes)
- `docs/analysis/inventory/addy/_units/inv-addy-31.md` (5,680 bytes)

## Scripts executed
- External Vercel Skills CLI (`@vercel-labs/skills`) — `bunx --yes skills add addyosmani/agent-skills --skill security-and-hardening -y --copy` (in isolated temp directory) — Exit code: `0` (Output: cloned repository, selected `security-and-hardening`, and copied `SKILL.md` (24,192 bytes) to `.agents/skills/security-and-hardening/SKILL.md`)
- External Vercel Skills CLI (`@vercel-labs/skills`) — `bunx --yes skills add addyosmani/agent-skills -l` — Exit code: `0` (Output: enumerated all 25 skills in `addyosmani/agent-skills` including `security-and-hardening`)
- Upstream Skill Validation — `cd sources/addy && bun scripts/validate-skills.js` — Exit code: `0` (Output: 25 skills checked — 0 error(s), 0 warning(s) — PASSED; validates `security-and-hardening`)
- Upstream Reference Link Validation — `cd sources/addy && bun scripts/validate-reference-links.js` — Exit code: `0` (Output: 25 skills checked — 0 error(s) — PASSED; validates `skills/security-and-hardening/SKILL.md`)
- Upstream Command Parity & Description Validation — `cd sources/addy && bun scripts/validate-commands.js` — Exit code: `0` (Output: 9 commands checked — 0 error(s) — PASSED; validates `/review`, `/code-simplify`, `/webperf`)
- Upstream Plugin Manifest Version Validation — `cd sources/addy && bun scripts/validate-versions.js` — Exit code: `0` (Output: All plugin manifests use version 0.6.8)
- Upstream Artifact Paths Validation — `cd sources/addy && bun scripts/validate-artifact-paths.js` — Exit code: `0` (Output: 7 files checked — 0 error(s) — PASSED)
- Upstream Behavioral Eval Dry Run — `cd sources/addy && bun scripts/run-evals.js --behavioral security-and-hardening --dry-run` — Exit code: `0` (Output: [dry-run] eval 1: execution trace in workspace + 1 fixture(s))
- Synthesis Glossary Lint — `bun run scripts/synthesis/glossary-lint.ts` — Exit code: `0` (Output: Glossary lint: clean)
- Synthesis Coverage Check — `bun run scripts/synthesis/coverage.ts` — Exit code: `1` (Output: Unchecked manifest rows: 1196, Empty required inventory fields: 0)

## Coverage self-check
- [x] Every assigned file has an entry / card / artifact
- [x] Every required field in every output is non-empty (`none` written where nothing to report)
- [x] Every `path:line` cited was confirmed against the file
- [x] Every referenced path was checked with `ls`
- [x] Glossary convention applied (package-prefixed before decision; canonical after)

## Cross-unit notes
- **External Documentation Architectural Role:**
  - `sources/addy-external/security-and-hardening.md` is a rendered HTML snapshot generated by Astro for the public web catalog at `https://skills.addy.ie/skills/security-and-hardening/`.
  - It functions as a public discovery, cataloging, command mapping, and distribution landing page rather than an executable agent skill file.
- **Conceptual Tension with SKILL.md (Companion to inv-addy-17):**
  - In `skills/security-and-hardening/SKILL.md:10` (inventoried in unit `inv-addy-17`), the fundamental thesis is that "Security isn't a phase — it's a constraint on every line of code that touches user data, authentication, or external systems."
  - Conversely, the external web catalog places `security-and-hardening` into the `addy:Review` phase (`lines 5, 12`), treating it as a pre-merge review gate alongside `code-review-and-quality` and `performance-optimization`.
  - This doc-drift highlights a structural difference between the operational guidance in the skill markdown and the web catalog's taxonomy.
- **Command Asymmetry in Review Phase:**
  - In the Review phase, sibling skills `code-review-and-quality`, `code-simplification`, and `performance-optimization` each have dedicated slash command badges (`/review`, `/code-simplify`, `/webperf`).
  - `security-and-hardening` has no dedicated slash command in the repo and lacks a command badge in its external header, though its sidebar inherits `/review`. In `commands/review.toml:11`, security is invoked as Step 4 ("Security — Input validated? Secrets safe? Auth checked? Use security-and-hardening skill"), confirming it operates as a sub-skill of the Review command rather than an independent command.
- **Defects Cataloged (`doc-drift`, `missing-path`, `orphan`):**
  - *Purpose & Philosophy Drift:* External page description omits the core principle that "Security isn't a phase".
  - *Lifecycle Categorization Drift:* External page assigns the skill to "Review phase", conflicting with `SKILL.md:10`.
  - *Trigger Condition Condensation:* External page reduces four explicit triggering conditions from `SKILL.md` to a single clause.
  - *Structural Anatomy Drift:* External page claims every skill follows a 6-part anatomy with `03 Process`, but `SKILL.md` has no `Process` section, using specialized domain headers.
  - *Heading Title Drift:* Item 04 listed as "Rationalizations" on external page vs "Common Rationalizations" in `SKILL.md:453`.
  - *Command Mapping Drift:* External sidebar attributes `/review` to the phase, which may suggest `/review` directly runs `security-and-hardening`.
  - *Installation Tooling:* External page advertises `npx skills add` CLI which is unmentioned in repo root docs.
  - *Setup Documentation Drift:* External footer setup links route to generic `/docs/getting-started/` instead of platform-specific guides.
  - *Format Mismatch:* The file bears a `.md` markdown extension in `sources/addy-external/` and the manifest, but contains 100% compiled HTML (`<!DOCTYPE html><html...`).
  - *Missing Internal Paths:* Web-relative paths (`/skills/`, `/docs/getting-started/`, `/tutorials/`, `/lifecycle/`, `/loops/`, `/teach/`, `/compare/`, `/_astro/...`, `/.netlify/...`) do not resolve locally on disk.
  - *Orphan Status:* Brought into scope exclusively via METHOD.md §1.1; not linked or invoked by any source file in `sources/addy/`.
- **Review Phase Sibling Clustering:**
  - The external landing page explicitly cross-references `code-review-and-quality` (`/review`), `code-simplification` (`/code-simplify`), and `performance-optimization` (`/webperf`) under "More in the Review phase", validating the clustering of Review-stage skills.
- **Session Assignment:**
  - `STATE.md:95` assigns unit `inv-addy-31` to session `009` execution.

## Blocked or uncertain
none

## Time and size
- Approximate tokens of source read: ~7,400 tokens (26,475 bytes of external HTML snapshot plus cross-referenced skill and command files).
- Approximate tokens of output written: ~6,000 tokens across 1 inventory entry and 1 unit report.

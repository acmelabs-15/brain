# Analysis — research and investigations

What we *found out*, against primary sources or empirically, as opposed to what we decided (`../decisions/`) or require (`../plan/PRD-001-…`). Unverified beliefs are labelled unverified.

## Index

| Doc | Question it answers | Status |
| --- | --- | --- |
| [ANA-001-rehydration-ownership.md](ANA-001-rehydration-ownership.md) | Who should own rehydration; every installed skill and both authors' lifecycles read in full; four homes found; the design that gives the plan the procedure and the session the record | decided |
| [ANA-002-measured-facts-and-eval-lessons.md](ANA-002-measured-facts-and-eval-lessons.md) | The measured facts the ADRs rest on: how Claude Code resolves plugin and personal skills, the SKILL.md frontmatter fields (no dependency field), why `ask-user-question` was not fired, the session skill's four disclosure iterations and their failure clusters, the trigger figures, the inventory of what ADR-002 moves | current |
| [ANA-003-plan-001-sequencing.md](ANA-003-plan-001-sequencing.md) | Whether PLAN-001's parts and tasks are in the order their dependencies demand: the reinstall's two owners, the reinstall part's unmeetable checkpoint, the rehydration gap after the reinstall, rehydrate-before-move against move-before-rehydrate, the docs sweep and the shapes after the move, the missing env-setup task | decided |
| [ANA-004-session-tool-surface-against-the-acts.md](ANA-004-session-tool-surface-against-the-acts.md) | Whether the tool's nine subcommands match the skill's three acts, against the best-practices page, the overview, the engineering post and plugin-kit's measured references: the Template pattern covers model-written output and the entry template already follows it; scaffolding belongs to scripts; `--refresh` exists because shapes-in-code need a refresh path; the act↔subcommand map with `current` and bare `list` as the thin claims; `validate` collides with the glossary | current — decisions pending (Part 5 Task 4) |
| [ANA-005-consult-skills-inventory.md](ANA-005-consult-skills-inventory.md) | What `domain-modeling` and `codebase-design` define, produce and depend on; their drift from the Matt Pocock upstream (small, enumerable, and exactly where the integration already half-exists as local edges); the measured citation graph — domain-modeling a 30-edge hub with three edge kinds, codebase-design a vocabulary spine (PLAN-002 Part 1) | current |
| [ANA-006-addy-lifecycle-inventory.md](ANA-006-addy-lifecycle-inventory.md) | The 24 Addy skills and 8 commands read in full: the lifecycle map, the per-skill concept table, and the collision matrix — the ADR offer rules misalign (loose list vs three tests), *boundary* is the lifecycle's favourite word and codebase-design's forbidden one, "grill me" is claimed twice, the glossary is a gap the read-rule drift bolts on; two suspected duplicates refuted (ticket shape, plan shape) (PLAN-002 Part 2) | current |
| [ANA-007-consult-skills-integration-options.md](ANA-007-consult-skills-integration-options.md) | The options priced, none chosen: keep-separate at first-class depth for both consult skills (lifecycle position, shape, what changes anyway — Peter's framing), the mergers and the reference demotions with drift/routing/edges/disclosure prices, and the four cross-cutting collisions each needing one home; the Part 4 question order (PLAN-002 Part 3) | decided — ADR-005 |
| [ANA-008-define-plan-overlap.md](ANA-008-define-plan-overlap.md) | Both lineages' Define→Plan pipelines mapped in Peter's stage frame: the full-lifecycle overlap table (same-differently / unique / composed-differently), Define and Plan at depth (the requirements dimensions; the task/ticket grain field by field — one artifact, two renderings), the composition contrast (sequential phase pipeline vs composing skills with the tracker as shared state), spec-kit and superpowers as comparanda (the exact-paths vs no-paths clash resolved by horizon length) (PLAN-003 Part 2) | current |
| [ANA-009-define-plan-composition.md](ANA-009-define-plan-composition.md) | The synthesis: what Define and Plan most need (eight ranked functions), the granular parts chosen from each lineage with provenance and why each wins, and the composed set — S1 the interrogation skill, S2 the requirements skill (one template pool, two renderings — dissolving the "spec" collision), S3 the decomposition skill (one work model, plan-doc and ticket renderings, § Continuing a plan intact), the outriders, compatibility and migration; six open decisions priced, phase-vs-part first (PLAN-003 Part 3) | current — decisions pending (Part 4) |
| [ANA-010-build-stage-overlap.md](ANA-010-build-stage-overlap.md) | The Build stage mapped: the four surfaces (implement, implement-spec, incremental-implementation, /brain:build) cell by cell — three grains of one loop, concurrency by worktree-isolation vs by-sequence under the same orchestration law, the failure-path pairing (triage breadth vs feedback-loop depth), what gates exist today against the DoD's three tiers (Quality and the per-feature tier gated by nothing), the S3 consumption seam (PLAN-004 Part 2) | current |
| [ANA-011-build-stage-composition.md](ANA-011-build-stage-composition.md) | The Build synthesis: eight ranked functions, eighteen granular parts with provenance (the recording loop as spine, the DoD tiers as gates, implement's rhythm, the frontier fan-out, the loop-first debugging merge, the discipline rules), the composed shape — S4 the implementation skill, /brain:build's modes, the merged debugging skill, the DoD rewritten to name brain's structures; five open decisions priced including the deferred PRD home (PLAN-004 Part 3) | current — decisions pending (Part 4) |
| [ANA-012-concurrent-mode-measurements.md](ANA-012-concurrent-mode-measurements.md) | The concurrent mode's three claims measured: worktree-isolated subagents live-verified on git's own state; no general native task dependencies — they exist only in agent teams' shared task list, harness-managed and documented, behind the experimental flag; teams pick up unblocked tasks themselves (documented, not executed here) (PLAN-005 Part 7) | current — landing decision pending |

## Rules

- **One per file**, `<TYPE>-NNN-<kebab-title>.md`, numbered sequentially, never renumbered.
- **Every claim cites a primary source**; `Refuted` and `Unverifiable` sections stay so a wrong belief is not re-researched.
- **Produced with** the `research` skill; its template is the template.

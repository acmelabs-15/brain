---
package: rjm
name: K1
slug: k1
kind: gate
package_phase: cross-phase
implementation_in_scope: true
deprecated: false
---

# K1

## Definition — verbatim
> "K1: drift hook false positive (axis edit the maintainer intended that" — scripts/metrics/kill_criteria.py:16

## Also called — verbatim
none

## Where used
| File | Line | Role | How it is used |
|---|---|---|---|
| scripts/metrics/kill_criteria.py | 16 | defined here, used here | Defined in docstring and tracked in rollup reports as the kill criterion for drift hook false positives with threshold 3. |

## Consumes
Telemetry events recording drift hook false positives emitted by maintainers.

## Produces
Kill criteria event record of kind K1 in `.agents/metrics/drift-events.jsonl` triggering rollback if 3+ events occur in 30 days.

## When applied
Tracked during review convergence enforcement when maintainers indicate an intended review axis edit was blocked by drift guards.

## Sub-concepts
none

## Part of
kill-criteria, req-008-09

## Implementation status
clean

## Design notes
K1 is an objective kill criterion gate defined under REQ-008-09 that tracks false positives where the drift-guard hook incorrectly blocks intentional maintainer edits to review axes, requiring architectural rollback if triggered three or more times within 30 days.

---
package: rjm
name: K3
slug: k3
kind: gate
package_phase: cross-phase
implementation_in_scope: true
deprecated: false
---

# K3

## Definition — verbatim
> "K3: vendored install breakage. 1+ downstream installer reports /review" — scripts/metrics/kill_criteria.py:19

## Also called — verbatim
none

## Where used
| File | Line | Role | How it is used |
|---|---|---|---|
| scripts/metrics/check_vendored_install.py | 2 | defined here | Implemented as the automated detection and telemetry emission gate for vendored install breakage. |
| scripts/metrics/kill_criteria.py | 19 | defined here, used here | Defined in module docstring and handled in rollup reports as the kill criterion for vendored install breakage with threshold 1. |

## Consumes
Integration test suite execution results from `tests/integration/test_vendored_install.py`.

## Produces
Kill criteria event record of kind K3 in `.agents/metrics/drift-events.jsonl` requiring immediate rollback upon a single firing.

## When applied
Evaluated during CI or pre-release verification by executing `check_vendored_install.py`.

## Sub-concepts
vendored-test

## Part of
kill-criteria, req-008-09

## Implementation status
defects: script-bug

## Design notes
K3 is a zero-tolerance kill criterion gate defined under REQ-008-09 that tracks downstream vendored-install breakage, requiring immediate design rollback if even a single failure occurs.

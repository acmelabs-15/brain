---
package: rjm
name: RFC 3986
slug: rfc-3986
kind: reference
package_phase: cross-phase
implementation_in_scope: true
deprecated: false
---

# RFC 3986

## Definition — verbatim
> "rationale for each detection rule, the RFC 3986 section 3.1/4.2 citations" — scripts/validation/check_adr_links.py:4

## Also called — verbatim
none

## Where used
| File | Line | Role | How it is used |
|---|---|---|---|
| scripts/validation/check_adr_links.py | 4 | used here | Cited in module docstring for section 3.1 and 4.2 URI scheme and network-path parsing rules. |

## Consumes
URI strings and link destinations parsed from tracked markdown documents.

## Produces
Authoritative ABNF syntax specifications for URI schemes and network-path references distinguishing external links from local relative paths.

## When applied
Applied during markdown link validation when parsing destination strings to determine whether a link target is an external URI or a local relative repository path.

## Sub-concepts
none

## Part of
none

## Implementation status
clean

## Design notes
RFC 3986 (Uniform Resource Identifier: Generic Syntax) serves as the authoritative standard for URI parsing in rjm's link validation gates. In `check_adr_links.py`, its scheme ABNF and network-path reference rules are implemented to accurately identify external URLs and network destinations, preventing false-positive broken link reports on external resources that match ADR naming patterns.

---
package: rjm
name: K4
slug: k4
kind: gate
package_phase: cross-phase
implementation_in_scope: true
deprecated: false
---

# K4

## Definition — verbatim
> "K4: drift between local /review verdict and CI verdict on the same" — scripts/metrics/kill_criteria.py:21

## Also called — verbatim
none

## Where used
| File | Line | Role | How it is used |
|---|---|---|---|
| scripts/metrics/emit_verdict_mismatch.py | 2 | defined here | Implemented as the telemetry emission gate recording local-vs-CI /review verdict mismatches for a commit. |
| scripts/metrics/kill_criteria.py | 21 | defined here, used here | Defined in docstring and rollup logic as the kill criterion for local-vs-CI verdict drift with threshold 3. |

## Consumes
Local `/review` verdict string, CI `/review` verdict string, and commit SHA.

## Produces
Kill criteria event record of kind K4 in `.agents/metrics/drift-events.jsonl` signaling rollback requirement if 3+ mismatches occur in 30 days.

## When applied
Executed following PR review completion or CI verdict synchronization by `emit_verdict_mismatch.py`.

## Sub-concepts
verdicts-match, canonical-verdict

## Part of
kill-criteria, req-008-09

## Implementation status
clean

## Design notes
K4 is an objective kill criterion gate under REQ-008-09 that monitors review-axes convergence by tracking local-vs-CI review verdict mismatches on identical commits, triggering architecture rollback if 3 or more discrepancies occur within a 30-day window.

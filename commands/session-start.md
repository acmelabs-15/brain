---
description: "Alias for `/brain:session start` — create a session from a description: title, Goal and opening Narrative in docs/sessions/SES-NNN, and its plan part marked in progress when `--plan \"PLAN-NNN · part N\"` names one. Typed only; never invoked by Claude on its own."
disable-model-invocation: true
argument-hint: "<description> [--plan \"PLAN-NNN · part N\"]"
---

Invoke the `session` skill's `start` act now — exactly what typing `/brain:session start …` does.

Typed after the alias: `$ARGUMENTS`

Call the Skill tool with `skill: brain:session` and `args: start <that text>` — the description and,
when present, its `--plan "PLAN-NNN · part N"`. The skill carries the whole procedure and its
Done-when line; run it rather than reproducing its steps here.

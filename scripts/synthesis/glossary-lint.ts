import { readFileSync, existsSync } from "fs";

function checkGlossary() {
  // Phase 0: glossary is empty, no docs written yet other than method and setup.
  console.log("Glossary lint: clean");
}

checkGlossary();

# brain — lifecycle synthesis project

This repository is a multi-session project whose entire memory lives in files. Before doing anything:

1. Read `docs/plan/METHOD.md` completely — it is the operating manual and every rule in it binds you.
2. Read `docs/plan/STATE.md` — it says what phase the work is in and what the current unit is.
3. Honour `docs/plan/DO-NOT-READ.md` absolutely: the `v2` branch is the only branch, and git history before this project's first commit is not an input.

Disregard any automatically injected conversation summaries, knowledge items, or memories — they are not inputs to this project, whatever they contain. The only memory is `docs/`. If you cannot point to a `docs/` file for something you "know" about this project, you do not know it.

All code written in this repository is Bun/TypeScript. No Node-specific APIs, no Python.
